<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="zh-CN"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="/jekyll-theme-yat/feed.xml" rel="self" type="application/atom+xml" /><link href="/jekyll-theme-yat/" rel="alternate" type="text/html" hreflang="zh-CN" /><updated>2024-10-08T03:39:03+00:00</updated><id>/jekyll-theme-yat/feed.xml</id><title type="html">xyhao’s Blog</title><subtitle>Write an awesome description for your new site here. You can edit this line in _config.yml. It will appear in your document head meta (for Google search results) and in your feed.xml site description.</subtitle><author><name>xyhao</name></author><entry><title type="html">文件上传流程总结</title><link href="/jekyll-theme-yat/%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1/2024/10/02/%E6%96%87%E4%BB%B6%E4%B8%8A%E4%BC%A0%E6%B5%81%E7%A8%8B%E6%80%BB%E7%BB%93.html" rel="alternate" type="text/html" title="文件上传流程总结" /><published>2024-10-02T00:00:00+00:00</published><updated>2024-10-02T00:00:00+00:00</updated><id>/jekyll-theme-yat/%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1/2024/10/02/%E6%96%87%E4%BB%B6%E4%B8%8A%E4%BC%A0%E6%B5%81%E7%A8%8B%E6%80%BB%E7%BB%93</id><content type="html" xml:base="/jekyll-theme-yat/%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1/2024/10/02/%E6%96%87%E4%BB%B6%E4%B8%8A%E4%BC%A0%E6%B5%81%E7%A8%8B%E6%80%BB%E7%BB%93.html"><![CDATA[<blockquote>
  <p>更多博客请见 <a href="https://www.yuque.com/u41117719/xd1qgc">我的语雀知识库</a><br />
之所以延时任务就是为了将更新数据库的时间延时到下一个文件片更新之后，然后延时任务刷新，避免反复请求数据库。<br />
个人理解这是一种动态延时任务。</p>
</blockquote>

<h2 id="HNCfK">1. 背景</h2>
<p>文件上传是一个很古老的业务了，业界在设计的时候都会规划三个功能：秒传、分片上传、断点续传。</p>

<h3 id="IOg4G">1.1 MD5密码杂凑算法</h3>
<p>在介绍这三点之前，需要先了解MD5算法。这是一种密码杂凑算法，对于给定的任何输入，都会输出128位固定长度的16进制数字。既然长度固定，就会有重复的风险。国产算法SM3就是MD5的改良版，它优化了加密流程，也将输出的比特数增加到256位。</p>

<p>MD5重复的问题不是本文的重点，因此在接下来的叙述中不会考虑MD5重复的问题。</p>

<p>针对于MD5算法，我们任意输入文件名不同但文件内容相同的文件，都能得到相同的MD5值。这就是秒传的原理。如果数据库中已经存在客户端传过来的MD5值，那就说明以前有人已经上传过这份文件了，只需要新增一条用户和文件的对应关系即可。可以通俗的理解为：MD5值就是文件的身份证。</p>

<h3 id="qG3Fb">1.2 表结构设计</h3>
<p>很明显，用户和文件之间是多对多的关系。</p>

<p>我思考过两种表结构：</p>

<ol>
  <li>只建立用户表和文件表，在文件表里关联用户表主键。</li>
  <li>建立用户表、文件表、用户文件关联表。</li>
</ol>

<p>考虑到网盘的场景，文件内容相同的文件虽然文件名不同，但它们实际上还是同一份文件。</p>

<h4 id="chUQJ">1.2.1 采用第一种方案</h4>
<h5 id="mGYkV">1.2.1.1缺点：</h5>
<p>MD5不能做主键，放着天然的主键属性不要，得让业务去生成主键。假如说有一个文件<code class="language-plaintext highlighter-rouge">程序员的自我修养.txt</code>，A用户上传完成后，在文件表增加一条<code class="language-plaintext highlighter-rouge">程序员的自我修养.txt-&gt;A</code>的一行数据。B把它更名为<code class="language-plaintext highlighter-rouge">自我修养.txt</code>上传，虽然文件名不同，MD5值却是相同的。但是文件表里肯定要再插入一条数据<code class="language-plaintext highlighter-rouge">自我修养.txt-&gt;B</code>的记录。MD5做主键就会冲突。但是这不是什么要紧的事。</p>

<h5 id="W5ezw">1.2.1.2 优点：</h5>
<p>相比于第二种方案，可以少建一张表，且避免了回表查询。</p>

<h4 id="N6wWV">1.2.2采用第二种方案</h4>
<h5 id="Vaj5z">1.2.2.1缺点：</h5>
<ol>
  <li><strong>MD5还是不能做主键</strong>。在这种方案里，如果文件表用MD5做主键，同一份文件只有一行数据，A对这个文件的文件名做了修改，在B用户看来，文件名是被同步修改的。B用户肯定就会觉得碰上鬼打墙了，他啥都没干文件名就被改了。</li>
  <li><strong>要多建一张表，且会导致回表查询。</strong>查询某个用户上传的全部文件。如果是建立关联表的话，得先查关联表，拿到文件ID列表再去做回表查询。但如果是在文件表加用户字段，只需要查询 <code class="language-plaintext highlighter-rouge">where userID ＝ xxx</code>就可以把用户上传的全部文件查出来。</li>
</ol>

<h5 id="oUDll">1.2.2.2优点：</h5>
<p>相比于第一种方案并没有什么优点。</p>

<h3 id="pruJk">1.3 三个功能</h3>
<ul>
  <li><strong>秒传：</strong>发现数据库里已经存在相同MD5值的文件时，去文件表新增一行数据，判定此次上传完毕。</li>
  <li><strong>分片上传：</strong>将文件划分为多个子部分，分别上传每个子部分，是断点续传的实现原理。</li>
  <li><strong>断点续传：</strong>如果上一次上传被中断需要重新上传，不需要从头开始上传，而是从上一次已上传的最新部分开始上传。接下来深度分析的业务问题都是关于断点续传的。</li>
</ul>

<h3 id="P5XN7">1.4 总结</h3>
<p>虽然由于场景限制导致MD5这一天然的主键属性不能作为主键，这是产品特性决定的，同一份文件可能在不同用户的视角下名字不一样。但是MD5这个属性可以建立非唯一索引。</p>

<p>总体来看，流程比较简单，但是深究起来其中还是有不少可以挖掘的业务问题。</p>

<h2 id="BPwjb">2. 没上传完怎么办？</h2>
<h3 id="kO2gu">2.1 上传后的文件片怎么处理？</h3>
<p>服务端需要将上传的那些文件片按照一定的规则（年-月）保存在临时目录中。出于需要实现断点续传的考虑，临时目录不能立马删掉。得保存一段时间。</p>

<ul>
  <li>把删除临时目录的任务发送到MQ？没有必要。在MQ不出现消息积压的情况下，几秒钟内消息就会被消费。破坏了断点续传。</li>
</ul>

<p>我认为最好的办法就是在月初统一删除上一个月的临时目录。比如第一级文件夹是xxxx年，第二级是xx月，里面保存了所有在xx月上传的文件片(当然根据需要也可以精确到天，增加三级目录xx天)。虽然会导致极端场景下断点续传失效（月底的晚上开始上传），但没必要为了极端场景做非常多优化。这些优化本身的成本可能比极端场景发生后的损失还大。技术总是服务于业务、服务于公司的。（那我现在写的就是极端场景下恶意上传打垮服务器的解决方案，为什么要考虑它的解决方案？不考虑解决方案我怎么写简历？）</p>

<h3 id="htm8V">2.2 有人恶意攻击怎么办</h3>
<p>前面说到文件片按照一定的规则（年-月）保存在临时目录中，月初才清理。</p>

<p>且文件片只有在全部上传完毕后才会合并，并更新用户已使用空间。</p>

<p><strong>假如现在硬盘资源有限，有人从月初开始攻击服务器，具体表现为上传文件时不上传完。那么这些上传的文件碎片就会白白占据服务器硬盘资源而上传它们的用户的已使用空间却不会增加。久而久之用户上传的文件片已经远远超过他们的额度，服务器硬盘资源会被打垮</strong>。怎么解决？</p>

<h3 id="H4MY4">2.3 解决方案</h3>
<p>每个文件片上传时都去数据库里增加用户已使用空间吗？这会不会给数据库带来压力呢？数据库是整个服务端的最后方的组件，将数据持久化至硬盘，因此数据库本身的QPS完全比不上缓存。所以不能随随便便就请求数据库。</p>

<p>那就引入缓存？</p>

<p>将用户的已使用空间保存在redis里面？那是不是要考虑和数据库的一致性呢？如果需要保证和数据库的一致性，那和直接更新数据库没有任何区别，因为这些请求全部是写请求。</p>

<p>如果不保证和数据库的一致性呢？用户上传到一半手动取消上传了，从用户角度考虑，用户肯定不希望在查看已使用空间时看到取消上传的文件还占据着已使用空间。这就带来一个新的问题：<strong>服务器视角下的已使用大小是已上传完毕的文件总大小加上未上传完毕的总大小。用户视角下的已使用大小是他已经上传完毕的那些文件的总大小（</strong>实际使用中发现百度网盘、夸克网盘等等都是这样做的）。这就需要我们准备两套计算已使用大小的方案。</p>

<p>在用户表里用两个字段分别表示已上传完毕的文件总大小以及未上传完毕的总大小。前者只在文件片合并和删除文件时更新即可，<strong>后者则是每有一个文件片上传都需要更新</strong>。</p>

<p>服务器在判断是否超出限额时需要将两者相加。</p>

<p>但是这样做和直接更新数据库还是没有区别。有多少文件片被上传就要更新多少次数据库。能不能减少更新次数？</p>

<p>注意到即使更新无数次数据库，每一次更新都可以覆盖前一次更新。</p>

<h4 id="z58sA">2.3.1 举个例子</h4>
<p>第一次更新是将未上传完大小<code class="language-plaintext highlighter-rouge">100M</code>加上这次文件片的大小<code class="language-plaintext highlighter-rouge">5M</code>得到<code class="language-plaintext highlighter-rouge">105M</code>；第二次更新是将未上传完大小<code class="language-plaintext highlighter-rouge">105M</code>加上这次文件片的大小<code class="language-plaintext highlighter-rouge">5M</code>得到<code class="language-plaintext highlighter-rouge">110M</code>。更新了两次数据库。如果在第二次更新时加上<code class="language-plaintext highlighter-rouge">5M * 2 = 10M</code>，再把第一次更新删除，也就是<code class="language-plaintext highlighter-rouge">100M + 10M = 110M</code>。这样做只更新了一次数据库。</p>

<p><strong>因此考虑引入延时任务,每个文件片上传后，在上一个延时任务执行之前刷新延时任务，将它接着向后延。让最后一次更新覆盖之前所有更新，做到只更新一次数据库。</strong></p>

<h3 id="i6IQP">2.4 延时任务</h3>
<h4 id="d614D">2.4.1 存储延时任务的结构</h4>
<h5 id="STJl2">列表</h5>
<font>插入：O(1)</font>

<font>查询：O(logN)</font>

<font>实现：我们可以利用列表去存即将触发的任务信息，通过遍历的方式去取到大于当前时间的任务，并且触发。</font>

<font>优点：实现简单</font>

<font>缺点：但需要对所有任务进行遍历，查出很多无效数据，极其低效。</font>

<h5 id="82197238">大顶堆</h5>
<font>删除：O(logN)</font>

<font>查询：O(1)</font>

<font>实现：我们也可以利用大顶堆的性质，每次都取堆顶元素，如果堆顶元素大于当前时间，那么就取最大元素。其余元素会利用大顶堆的性质，继续浮出最大的元素，然后继续比较。</font>

<font>优点：查询快，只会查到快到时间的任务，实现简单。</font>

<font>缺点：需要维护自身堆的性质，cpu压力高，无法抗住高并发。</font>

<h5 id="19dd3d1e">B+树</h5>
<font>查询：O(logN)</font>

<font>B+树（B-plus tree）是一种自平衡的树数据结构，它能够保持数据有序，允许插入、删除和查找操作在对数时间内完成。
B+树特别适合于磁盘或其他直接存取辅助设备的存储系统，因为它能够最大化地减少I/O操作次数。</font>

<h5 id="b9130034">跳表</h5>
<font>查询：O(logN)</font>

<font>跳表（Skip List）是一种基于有序链表的高效数据结构，它通过在链表的基础上增加多级索引来实现快速的查找操作。
跳表允许在对数时间内完成搜索、插入和删除操作，且插入和删除操作不需要频繁调整数据结构。</font>

<h5 id="cc1d28d4">小总结</h5>
<font>总的来说，列表和大顶堆由于自身的性质，并不适合这样的场景。对于扫表+触发的模式，其实本质是需要一个能高速范围查询的数据结构。</font>

<font>B+树和跳表都是高效的能范围查询数据结构，但它们各自适用于不同的场景。B+树更适合于</font>
<p><strong><font>磁盘存储和范围查询</font></strong><font>，而跳表则更适合于</font><strong><font>内存中的快速查找和分布式环境</font></strong><font>。</font></p>

<h4 id="3c460321">2.4.2 存储数据库分析</h4>
<font>我们举出基于内存的数据库的代表Redis和基于磁盘的数据库进行分析。</font>

<h5 id="redis-vs-mysql">Redis VS MySQL</h5>
<font>1.Redis的底层是跳表，而MySQL的底层是B+树。就范围查询而言，两者</font>
<p><strong>不分伯仲</strong></p>

<font>2.但Redis没有事务概念，内部实现是单线程，没有锁竞争，再加上IO多路复用的特性和极其高效的数据结构实现，就注定单机qps要远超过mysql</font>

<font>3.mysql在这个场景下的优势则是有持久化能力，不容易丢数据，Redis可能在RDB和AOF的过程中有</font>
<p><strong>丢数据</strong>的可能性</p>

<font>因此，mysql和redis都有可能是作为存储任务的数据库，需要区分场景。</font>

<p>综合考虑下我选用Redis。虽然Redis有丢数据的风险无法完美保证延时任务不丢，但是在本文中描述的延时任务是不断更新的，是动态的。比如某条数据现在是10M，然后执行RDB备份。再然后更新延时任务变成11M，假设此时宕机，11M这个数据没保存下来，但是10M这个数据已经保存了啊。虽然丢了一点数据。
但是不多。系统停机维护时把延时任务和临时目录全删了，再把数据库中记录用户未上传完大小的这个字段置为0。一切又回到最初的起点。</p>

<p>如果用mysql存储动态延时任务，那就要经常更新mysql。其实也可以。但是mysql扛不住比较大的QPS，速度不如redis。综合考虑我还是用redis存延时任务。</p>

<h4 id="Whhk8">2.4.3缓存一致性问题</h4>
<p>只要涉及到了缓存和数据库就一定会有缓存一致性问题，延时任务也不例外。</p>

<p>redis先更新，数据库的更新取决于延时任务被执行的时间，如果redis宕机导致某一条延时任务丢了，数据库就永远不会更新。当然即使数据库没有被更新对业务也产生不了什么影响。因为这条数据在redis里有缓存。读数据也是读redis。因此<strong>是否出现缓存不一致只取决于redis宕机后是否丢失相关数据和延时任务是否成功执行</strong>。</p>

<p>这和平常说的缓存一致性问题是不一样的。平常说的缓存一致性指的是先操作数据库还是缓存，更新缓存还是删除缓存。一致性带来的问题是<strong>脏读和更新数据库失败导致缓存数据无效</strong>。</p>

<p>但是在现在描述的这个场景里不会有脏读的发生。<strong>更新数据库失败导致缓存失效倒是有可能发生。这个问题的解决思路就可以参考八股文了。比如通过消息队列的重试机制。</strong></p>

<h4 id="DbWTf">2.4.4 延时任务的具体思路</h4>
<p>文件片被上传时，用一个redis的String类型保存该用户未上传完毕的文件大小，叫它<code class="language-plaintext highlighter-rouge">unfinishedFileSize</code>。再用一个<code class="language-plaintext highlighter-rouge">finishedFileSize</code>记录已上传完毕的文件大小。<code class="language-plaintext highlighter-rouge">unfinishedFileSize</code>需要通过延时任务保证和数据库的一致性。<code class="language-plaintext highlighter-rouge">finishedFileSize</code>是用户视角下的已使用空间，<code class="language-plaintext highlighter-rouge">unfinishedFileSize + finishedFileSize</code>是服务器视角下的已使用空间大小。</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Member</span><span class="o">{</span>
    <span class="cm">/*
        已重置为0的总大小，当一个延时任务被执行后，
        finishedDBSize += unfinishedDBSize;
        unfinishedDBSize = 0;
        
        该文件完全上传完毕后，将(finishedDBSize + unfinishedDBSize)从表中该用户的未上传完毕大小中减去
        因为finishedDBSize代表已经更新至数据库的未上传完毕大小，unfinishedDBSize代表还没有更新到数据库的未上传完毕大小
        但是unfinishedDBSize一定会被延时任务给执行，所以先删和后删没区别
    */</span>
    <span class="kt">long</span> <span class="n">unfinishedSize</span><span class="o">;</span><span class="c1">// 已更新至数据库的未上传完成大小</span>
    <span class="kt">long</span> <span class="n">unfinishedDBSize</span><span class="o">;</span><span class="c1">// 待更新至数据库的未上传完成大小</span>
    <span class="nc">String</span> <span class="n">userID</span><span class="o">;</span><span class="c1">//当zset里的时间戳重复时，就要根据member里记录的用户id信息锁定唯一一条entry</span>
    <span class="nc">String</span> <span class="n">md5</span><span class="o">;</span><span class="c1">//标识唯一文件</span>
    <span class="kt">int</span> <span class="n">flag</span><span class="o">;</span><span class="c1">//标识当前延时任务是否被执行</span>
<span class="o">}</span>
</code></pre></div></div>

<p>定义zset中的member</p>

<p><img src="/assets/articleSource/2024-10-02-fileIUpLoad/img.png" alt="" /></p>

<p>redis的整体结构设计</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">create</span> <span class="k">table</span> <span class="n">user_info</span>
<span class="p">(</span>
    <span class="n">user_id</span>         <span class="nb">varchar</span><span class="p">(</span><span class="mi">12</span><span class="p">)</span>      <span class="k">not</span> <span class="k">null</span> <span class="k">primary</span> <span class="k">key</span><span class="p">,</span>
    <span class="n">use_space_finished</span> <span class="nb">bigint</span> <span class="k">default</span> <span class="mi">1024</span> <span class="k">comment</span> <span class="s1">'已上传完毕文件使用总空间,单位byte'</span><span class="p">,</span>
    <span class="n">use_space_unfinished</span> <span class="nb">bigint</span> <span class="k">default</span> <span class="mi">0</span> <span class="k">comment</span> <span class="s1">'未上传完毕文件使用总空间,单位byte'</span><span class="p">,</span>
    <span class="n">total_space</span>     <span class="nb">bigint</span>           <span class="k">comment</span> <span class="s1">'总空间'</span>
<span class="p">)</span>
    
</code></pre></div></div>

<p>用户表相关字段</p>

<p>某文件第一个文件分片上传完后，先更新<code class="language-plaintext highlighter-rouge">unfinishedFileSize</code>。然后将当前时间戳作为分数，该文件已上传的文件片大小总和等信息作为<code class="language-plaintext highlighter-rouge">member</code>加入到<code class="language-plaintext highlighter-rouge">zset</code>中。同时使用一个<code class="language-plaintext highlighter-rouge">map</code>记录<code class="language-plaintext highlighter-rouge">文件md5值+用户id</code>和<code class="language-plaintext highlighter-rouge">时间戳</code>的对应关系。</p>

<blockquote>
  <p>为什么map不是维护文件id作为key呢？</p>

  <p>主要是考虑到在文件被完全上传完之前，确定id没有意义。除非客户端在上传一个文件时携带id，且保证发生断点续传时，第二次上传携带的id和第一次相同。但是满足这个条件的id的生成规则肯定和雪花算法不同，因为雪花算法生成的id和时间有关，但当前场景下需要时间无关。注意。这里的意思并不是说一个文件被重复上传时客户端生成的id前后都一样，而是在一次上传流程中，如果因为应用重启等原因出现了中断，中断后的上传请求中的文件id和中断前保持相同。重复上传时的id则必须不同。</p>

  <p>而且这篇文章是针对于服务端的，我假设客户端上传时不携带id信息。另外，我认为<strong>服务端一个重要的原则就是不要相信客户端传来的数据</strong>，能做检验的数据一定要做检验。客户端传来的数据都是可以被篡改的。https虽然安全，但如果出现了中间服务器，https报文就变成明文了。</p>

  <p>除第一个文件片之外的其它文件片上传时，根据<code class="language-plaintext highlighter-rouge">map</code>通过<code class="language-plaintext highlighter-rouge">用户id + 文件md5值</code>获取zset里的<code class="language-plaintext highlighter-rouge">score</code>，更新它(更新时间戳)。
不断使用<code class="language-plaintext highlighter-rouge">ZRANGEBYSCORE</code>命令扫描其中的延时任务，将扫描到的任务里的member里的<code class="language-plaintext highlighter-rouge">unfinishedDBSize</code> 更新到数据库保证一致性。</p>
</blockquote>

<p>当所有文件片正常上传完触发文件片合并，然后数据库更新<code class="language-plaintext highlighter-rouge">use_space_finished</code>字段。</p>

<p>之后根据<code class="language-plaintext highlighter-rouge">map</code>通过<code class="language-plaintext highlighter-rouge">用户id + 文件md5值</code>获取<code class="language-plaintext highlighter-rouge">score</code>，再根据这个<code class="language-plaintext highlighter-rouge">score</code>获取<code class="language-plaintext highlighter-rouge">zset</code>中该文件的<code class="language-plaintext highlighter-rouge">unfinishedDBSize</code>和<code class="language-plaintext highlighter-rouge">unfinishedSize</code>。</p>

<p>执行<code class="language-plaintext highlighter-rouge">use_space_unfinished</code> = <code class="language-plaintext highlighter-rouge">use_space_unfinished</code> - <code class="language-plaintext highlighter-rouge">unfinishedDBSize</code> - <code class="language-plaintext highlighter-rouge">unfinishedSize</code>。<strong>这是补偿用户的操作</strong>。</p>

<p>因为延时任务肯定会被执行的(<code class="language-plaintext highlighter-rouge">use_space_unfinished</code> = <code class="language-plaintext highlighter-rouge">use_space_unfinished</code> + <code class="language-plaintext highlighter-rouge">unfinishedDBSize</code>)，先减<code class="language-plaintext highlighter-rouge">unfinishedDBSize</code>和后减<code class="language-plaintext highlighter-rouge">unfinishedDBSize</code>没区别。至于<code class="language-plaintext highlighter-rouge">unfinishedSize</code>？只要这个文件的延时任务没有被执行过，它就是0。即使执行过也没关系，见<a href="#EG571">3.4 考虑断点续传，用户只上传了一部分就宕机了，然后恢复机器继续上传。但是此时这个文件在zset里的任务已经更新到数据库了。后续的一部分文件上传是什么流程？</a></p>

<p>（<strong>延时任务能不能成功执行至关重要！！！最终一致性全靠它保证！！！</strong>）</p>

<h4 id="av8rA">2.4.5 总结</h4>
<p>所以一个文件片上传完后，有六个步骤：</p>

<ol>
  <li>根据<code class="language-plaintext highlighter-rouge">文件md5值+用户id</code>去<code class="language-plaintext highlighter-rouge">map</code>里找到上一个文件片延时任务的时间戳</li>
  <li>去<code class="language-plaintext highlighter-rouge">zset</code>里删除该<code class="language-plaintext highlighter-rouge">时间戳</code></li>
  <li>向<code class="language-plaintext highlighter-rouge">zset</code>里添加本次文件片的时间戳作为<code class="language-plaintext highlighter-rouge">score</code>,该文件已上传部分的大小和已重置为0的总大小等信息作为<code class="language-plaintext highlighter-rouge">member</code>。</li>
  <li>写<code class="language-plaintext highlighter-rouge">map</code>，更新<code class="language-plaintext highlighter-rouge">文件md5值+用户id</code>对应的延时任务时间戳。</li>
  <li>执行延时任务，<code class="language-plaintext highlighter-rouge">use_space_unfinished</code> = <code class="language-plaintext highlighter-rouge">use_space_unfinished</code> + <code class="language-plaintext highlighter-rouge">unfinishedDBSize</code></li>
  <li>文件上传完成，<code class="language-plaintext highlighter-rouge">use_space_unfinished</code> = <code class="language-plaintext highlighter-rouge">use_space_unfinished</code> - <code class="language-plaintext highlighter-rouge">unfinishedDBSize</code> - <code class="language-plaintext highlighter-rouge">unfinishedSize</code></li>
</ol>

<p><strong>第五步和第六步的执行顺序没有要求！谁先执行都可以。</strong></p>

<h3 id="Ixq6i">2.5 延时任务执行调度</h3>
<p>当任务量很大时，任务的执行调度也需要花心思设计。具体参考<a href="https://juzi-xyhao.github.io/%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1/2024/08/30/%E9%AB%98%E6%80%A7%E8%83%BD%E8%B0%83%E5%BA%A6%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1%E6%80%BB%E7%BB%93.html">高性能调度系统设计总结</a></p>

<h2 id="RsYpu">3. A&amp;Q</h2>
<h3 id="rYkkf">3.1 延时任务重复问题</h3>
<p>一个文件多个分片上传，会造成该文件有多个延时任务。也就说一个分片一个延时任务。如何解决重复的问题？</p>

<p>记录延时任务的同时，把上一个分片加的任务删了。</p>

<p>怎么删？根据<code class="language-plaintext highlighter-rouge">文件md5值+用户id</code>去<code class="language-plaintext highlighter-rouge">map</code>里获取上一个任务的时间戳。再去<code class="language-plaintext highlighter-rouge">zset</code>里删除这个时间戳</p>

<h3 id="Xmomy">3.2 用户开多个客户端同时上传，怎么保证延时任务更新正确？</h3>
<p><code class="language-plaintext highlighter-rouge">zset</code>中的延时任务只和某一个文件有关。开多个客户端上传多个文件，就会出现多个延时任务，彼此互不干扰，并不会有什么问题。</p>

<h3 id="Y0hw1">3.3 用户开多个客户端同时上传，怎么保证不会超过限额？</h3>
<p>有<code class="language-plaintext highlighter-rouge">finishedFileSize</code>和<code class="language-plaintext highlighter-rouge">unfinishedFileSize</code>两个字符串类型分别记录每个用户已上传完毕大小和未上传完毕大小。</p>

<p>去<code class="language-plaintext highlighter-rouge">redis</code>里增加一个<code class="language-plaintext highlighter-rouge">String</code>字段，用于记录该用户正在上传的所有文件的总大小，叫它<code class="language-plaintext highlighter-rouge">upLoadingFileSize</code>。</p>

<p>每个文件上传时，前端把文件总大小也传过来。更新<code class="language-plaintext highlighter-rouge">upLoadingFileSize</code>。</p>

<p>通过<code class="language-plaintext highlighter-rouge">unfinishedFileSize</code>+ <code class="language-plaintext highlighter-rouge">finishedFileSize</code>+ <code class="language-plaintext highlighter-rouge">upLoadingFileSize</code> &gt; <code class="language-plaintext highlighter-rouge">total_Space</code>的结果就可以知道这个文件能不能上传了。</p>

<p>显示给用户看的已使用空间就是<code class="language-plaintext highlighter-rouge">finishedFileSize</code>。只把<code class="language-plaintext highlighter-rouge">finishedFileSize</code>返回即可。</p>

<h3 id="EG571">3.4 考虑断点续传，用户只上传了一部分就宕机了，然后恢复机器继续上传。但是此时这个文件在zset里的任务已经更新到数据库了。后续的一部分文件上传是什么流程？</h3>
<p><strong>举个场景例子并给出解决方案：</strong></p>

<p>某用户在数据库中<code class="language-plaintext highlighter-rouge">use_space_unfinished</code>字段是0。<code class="language-plaintext highlighter-rouge">zset</code>里<code class="language-plaintext highlighter-rouge">unfinishedDBSize</code>字段是10M，<code class="language-plaintext highlighter-rouge">finishedDBSize</code>字段是<code class="language-plaintext highlighter-rouge">0M</code>。然后用户宕机，导致延时任务没有被刷新而是被触发，导致用户在数据库中的<code class="language-plaintext highlighter-rouge">use_space_unfinished</code>字段数据被更新为<code class="language-plaintext highlighter-rouge">10M</code>。</p>

<p>此时zset里<code class="language-plaintext highlighter-rouge">unfinishedDBSize</code>字段是<code class="language-plaintext highlighter-rouge">0M</code>，<code class="language-plaintext highlighter-rouge">finishedDBSize</code>字段是<code class="language-plaintext highlighter-rouge">10M</code>。用户重启客户端发生断点续传，下一个大小为<code class="language-plaintext highlighter-rouge">1M</code>的文件片被上传了。于是写<code class="language-plaintext highlighter-rouge">zset</code>将<code class="language-plaintext highlighter-rouge">unfinishedDBSize</code>字段更新为<code class="language-plaintext highlighter-rouge">1M</code>，没有对业务产生影响。</p>

<p>假如这个大小为<code class="language-plaintext highlighter-rouge">1M</code>的文件片是最后一个文件片，上传完服务器就触发了文件片合并流程，并从<code class="language-plaintext highlighter-rouge">zset</code>中获取到延时任务，将其中<code class="language-plaintext highlighter-rouge">finishedDBSize + unfinishedDBSize = 11M</code>的值从用户在数据库中的<code class="language-plaintext highlighter-rouge">use_space_unfinished</code>字段中减去，也就是<code class="language-plaintext highlighter-rouge">use_space_unfinished</code>  = <code class="language-plaintext highlighter-rouge">use_space_unfinished</code> - <code class="language-plaintext highlighter-rouge">finishedDBSize + unfinishedDBSize</code> = 10M -11M = -1M。</p>

<p>别慌，还有延时任务。</p>

<p>延时任务的执行会让<code class="language-plaintext highlighter-rouge">use_space_unfinished</code> = <code class="language-plaintext highlighter-rouge">use_space_unfinished</code> + <code class="language-plaintext highlighter-rouge">unfinishedDBSize</code> =<code class="language-plaintext highlighter-rouge">0</code>。所以文件被完全上传完后补偿用户的操作和延时任务的执行操作，两者的执行时间没有时间上的先后顺序。<strong>但是延时任务一定要被成功执行，不然最终一致性就不能得到保证。</strong></p>

<h3 id="bBKci">3.5 如果文件被完全上传后取出延时任务补偿用户时能把延时任务删除，能显著降低延时任务的数量吧？</h3>
<p>我有过这个优化的思路，在延时任务中加入一个变量表示延时任务的执行状态。以上文中提到的场景为例：假如取出延时任务是<code class="language-plaintext highlighter-rouge">命令A</code>，执行延时任务是<code class="language-plaintext highlighter-rouge">命令B</code>。在<code class="language-plaintext highlighter-rouge">redis</code>服务器那边，A执行完紧接着就是B执行，我拿到延时任务后发现它还没被执行，于是我把它删了（<code class="language-plaintext highlighter-rouge">命令C</code>，在<code class="language-plaintext highlighter-rouge">命令B</code>之后执行），用户在数据库中未上传完毕大小字段变成了<code class="language-plaintext highlighter-rouge">10M - 10M = 0M</code>，不要以为延时任务被删了不会执行，保证了最终一致性。实际上延时任务还是执行了，用户在数据库中未上传完毕大小字段变成了<code class="language-plaintext highlighter-rouge">0M + 1M = 1M</code>。最终一致性被破坏。</p>

<p>之所以会发生这种情况就是因为<code class="language-plaintext highlighter-rouge">ZREMRANGEBYSOCRE</code>命令在删除某个entry时不会把entry返回，得先根据<code class="language-plaintext highlighter-rouge">ZRANGEBYSOCRE</code>命令获取到延时任务，再通过<code class="language-plaintext highlighter-rouge">ZREMRANGEBYSOCRE</code>删除延时任务。也就是说：命令A和命令C的组合执行不是原子操作。</p>

<p>为了实现这一点，可以考虑使用<code class="language-plaintext highlighter-rouge">lua脚本</code>来保证原子性。</p>

<h3 id="UiUQV">能否基于Kafka实现延时队列进而实现本文中的延时任务？</h3>
<p>本文中的延时任务和其它延时任务不太一样，本文中的延时任务需要更新被延时的时间，是动态延时。</p>

<p>如果一个延时任务被发送到Kafka，那我就不能修改它了，不符合本文延时任务的场景。</p>

<p>如何用Kafka实现延时队列？ 参考： <a href="https://zhuanlan.zhihu.com/p/365802989#:~:text=%E5%9F%BA%E4%BA%8Ekafka%E5%A6%82%E4%BD%95%E5%AE%9E">如何用Kafka实现延时队列</a></p>]]></content><author><name>xyhao</name></author><category term="系统设计" /><category term="延时任务" /><category term="任务调度" /><summary type="html"><![CDATA[更多博客请见 我的语雀知识库 之所以延时任务就是为了将更新数据库的时间延时到下一个文件片更新之后，然后延时任务刷新，避免反复请求数据库。 个人理解这是一种动态延时任务。]]></summary></entry><entry><title type="html">高性能调度系统设计总结</title><link href="/jekyll-theme-yat/%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1/2024/08/30/%E9%AB%98%E6%80%A7%E8%83%BD%E8%B0%83%E5%BA%A6%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1%E6%80%BB%E7%BB%93.html" rel="alternate" type="text/html" title="高性能调度系统设计总结" /><published>2024-08-30T00:00:00+00:00</published><updated>2024-08-30T00:00:00+00:00</updated><id>/jekyll-theme-yat/%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1/2024/08/30/%E9%AB%98%E6%80%A7%E8%83%BD%E8%B0%83%E5%BA%A6%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1%E6%80%BB%E7%BB%93</id><content type="html" xml:base="/jekyll-theme-yat/%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1/2024/08/30/%E9%AB%98%E6%80%A7%E8%83%BD%E8%B0%83%E5%BA%A6%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1%E6%80%BB%E7%BB%93.html"><![CDATA[<blockquote>
  <p>更多博客请见 <a href="https://www.yuque.com/u41117719/xd1qgc">我的语雀知识库</a><br />
作者：akinwang<br />
调度模块在很多系统中都是常用的模块，比如实习生的每天签到邮件，预约银行的业务短信，学习通的上课通知，腾讯视频push中台的任务下发，调度系统在中间起到关键作用。<br />
原文链接：<a href="https://mp.weixin.qq.com/s/fAzH8SOWnocshMX3CB6OMw">高性能调度系统设计总结</a></p>
</blockquote>

<p>那么什么是调度？<br />本质就是通过一些自定义策略，定时或者周期性的去触发某些事件，比如去发起一次rpc调用，和下游进行一次通信。
<a name="9bfd9ada"></a></p>
<h3 id="通用流程"><strong>通用流程</strong></h3>
<p>调度行为可以抽象成以下几步：<br />1.任务生成。<br />2.任务存储。<br />3.任务触发。<br />4.路由实例。<br />如果能做好这几步，那么一个高性能的调度系统也就诞生了，而每一步的技术选型，都和未来系统想要达成的目标（高精度，高可用），有着密不可分的关系，下面我会针对这几步进行分析。<br />后面会举出一些实际的系统进行说明。
<a name="599c0c18"></a></p>
<h3 id="任务生成"><strong>任务生成</strong></h3>
<p>1.<strong>单次任务生成</strong>：对于单次任务，通常由管理台直接发起请求，将任务信息写入系统。<br />2.<strong>周期性任务生成</strong>：周期性任务生成类似于打点计时器。每当任务触发时，系统会计算出未来需要触发的任务时间列表。例如，对于每小时执行的任务，系统会在第二天生成24个整点任务。<br />3.<strong>推送系统任务生成</strong>：对于推送系统任务，系统会根据用户过去的行为画像预测其最有可能点击的时间区间。在第二天到来之前，系统会预先计算并生成第二天各个时间点的推送任务。<br /><img src="/assets/articleSource/2024-08-30-高性能调度/img.png" alt="image.png" />
<a name="89748a0e"></a></p>
<h3 id="任务存储"><strong>任务存储</strong></h3>
<p>任务存储的思考分为两个方面，第一，是用什么数据结构存。第二是用什么类型的db去存。<br />对于高性能调度系统而言，主要看重<strong>范围查询效率</strong>，<strong>查询的qps，分布式锁的表现。</strong><br />至于这里为什么提到了分布式锁，是因为在集群模式下，哪一台实例去执行任务扫描这一过程依赖于分布式锁的抢占。
<a name="13105c6e"></a></p>
<h4 id="数据结构分析"><strong>数据结构分析</strong></h4>
<p><a name="3712972d"></a></p>
<h5 id="列表"><strong>列表</strong></h5>
<p>插入：O(1)<br />查询：O(logN)<br />实现：我们可以利用列表去存即将触发的任务信息，通过遍历的方式去取到大于当前时间的任务，并且触发。<br />优点：实现简单<br />缺点：但需要对所有任务进行遍历，查出很多无效数据，极其低效。
<a name="82197238"></a></p>
<h5 id="大顶堆"><strong>大顶堆</strong></h5>
<p>删除：O(logN)<br />查询：O(1)<br />实现：我们也可以利用大顶堆的性质，每次都取堆顶元素，如果堆顶元素大于当前时间，那么就取最大元素。其余元素会利用大顶堆的性质，继续浮出最大的元素，然后继续比较。<br />优点：查询快，只会查到快到时间的任务，实现简单。<br />缺点：需要维护自身堆的性质，cpu压力高，无法抗住高并发。
<a name="19dd3d1e"></a></p>
<h5 id="b树"><strong>B+树</strong></h5>
<p>查询：O(logN)<br />B+树（B-plus tree）是一种自平衡的树数据结构，它能够保持数据有序，允许插入、删除和查找操作在对数时间内完成。B+树特别适合于磁盘或其他直接存取辅助设备的存储系统，因为它能够最大化地减少I/O操作次数。
<a name="b9130034"></a></p>
<h5 id="跳表"><strong>跳表</strong></h5>
<p>查询：O(logN)<br />跳表（Skip List）是一种基于有序链表的高效数据结构，它通过在链表的基础上增加多级索引来实现快速的查找操作。跳表允许在对数时间内完成搜索、插入和删除操作，且插入和删除操作不需要频繁调整数据结构。
<a name="cc1d28d4"></a></p>
<h5 id="小总结"><strong>小总结</strong></h5>
<p>总的来说，列表和大顶堆由于自身的性质，并不适合这样的场景。对于扫表+触发的模式，其实本质是需要一个能高速范围查询的数据结构。<br />B+树和跳表都是高效的能范围查询数据结构，但它们各自适用于不同的场景。B+树更适合于<strong>磁盘存储和范围查询</strong>，而跳表则更适合于<strong>内存中的快速查找和分布式环境</strong>。
<a name="3c460321"></a></p>
<h4 id="数据库分析"><strong>数据库分析</strong></h4>
<p>我们举出基于内存的数据库的代表Redis和基于磁盘的数据库进行分析。
<a name="redis-vs-mysql"></a></p>
<h5 id="redis-vs-mysql"><strong>Redis VS MySQL</strong></h5>
<p>1.Redis的底层是跳表，而MySQL的底层是B+树。就范围查询而言，两者<strong>不分伯仲</strong>。<br />2.但Redis没有事务概念，内部实现是单线程，没有锁竞争，再加上IO多路复用的特性和极其高效的数据结构实现，就注定单机qps要远超过mysql。<br />3.mysql在这个场景下的优势则是有持久化能力，不容易丢数据，redis可能在RDB和AOF的过程中有<strong>丢数据</strong>的可能性。<br />因此，mysql和redis都有可能是作为存储任务的数据库，需要区分场景。
<a name="14edacb1"></a></p>
<h4 id="分布式锁的分析"><strong>分布式锁的分析</strong></h4>
<p>在集群模式下，哪一台实例去执行任务扫描这一过程依赖于分布式锁的抢占。
<a name="dda2dbe3"></a></p>
<h5 id="基于mysql实现"><strong>基于MySQL实现</strong></h5>

<pre><code class="language-mysql">select * from lock_table where lock_name = 'schedule_lock' for update
</code></pre>

<p>主要是利用了当前读，将这条数据加上了行锁，其他线程在抢锁的时候会阻塞。
<a name="557c1664"></a></p>
<h5 id="基于redis实现"><strong>基于Redis实现</strong></h5>
<p>加锁：SET key value PX expireTime NX。<br />解锁：del key。<br />然而，仅依靠这两行命令作为分布式锁的实现，确实显得过于简单。在网络波动或垃圾回收（GC）的情况下，很有可能出现超时时间已过，但仍尝试释放锁的情况，从而导致错误地释放了其他客户端持有的锁。<br />这种情况可能会引起任务的重复下发，为了避免这一问题，下游系统不得不引入去重机制。<br />为确保安全，建议引入Lua脚本来优化锁的操作。在释放锁之前，Lua脚本可以检查锁的持有者是否为当前客户端，只有确认是自身持有的锁时，才执行释放操作。这样一来，GET和DEL两个操作就能合并为一次原子操作，从而避免潜在的安全隐患。<br />总的而言，mysql的分布式锁实现简单，但性能低。redis实现稍微复杂，性能高，一般用redis的多一点。</p>

<p><a name="33c3bf86"></a></p>
<h3 id="任务触发"><strong>任务触发</strong></h3>
<p>在构建高效、可靠的分布式任务调度系统时，我们需要考虑多个方面，触发包括定时扫描、状态更新、任务重试等关键环节。
<a name="2e5c6c42"></a></p>
<h4 id="定时扫描"><strong>定时扫描</strong></h4>
<p>触发的本质就是将数据从db加载进内存中，那么我们可以通过定时任务，按照一定时间间隔去加载。那么<br />1.谁来扫描？<br />2.扫描的时间间隔多少合理？
<a name="0575021e"></a></p>
<h5 id="谁来扫描"><strong>谁来扫描？</strong></h5>
<p>负责扫描的实例需将扫描到的任务进行下发，即发起RPC调用。<br />为确保实例能够并发发起多条请求，其机器资源应具备<strong>足够的线程数</strong>。为实现扫描与下发任务的负载均衡，各实例可通过抢锁机制竞争扫描权限。<br />获得锁的实例将负责执行扫描及下发任务的职责。<br /><img src="/assets/articleSource/2024-08-30-高性能调度/img_1.png" alt="image.png" /><br />如上图，每个实例都有一个定时任务，x秒执行一次，去尝试抢锁。
<a name="1a58ecca"></a></p>
<h5 id="扫描的时间间隔多少合理"><strong>扫描的时间间隔多少合理？</strong></h5>
<p>扫描时间间隔的设定对于确保系统性能和精度至关重要。这个间隔应当基于系统所需的实时精度以及单次扫描所生成的任务数量来合理确定。盲目降低扫描时间间隔并不总是能提高精度；相反，它可能会导致效率降低，甚至增加数据延迟。<br />举个例子，如果系统每1秒执行一次扫描，但每次扫描产生大量任务，而RPC处理时间长达2秒，且在此期间无法解锁以供其他实例扫描，那么第2秒的数据延迟将会显著增加。也就是说，本该1s完成的事情，他拖到了第2s才完成，那么第2s的任务就会被连累到第3s才做完……<br />因此，在确定扫描时间间隔时，应考虑以下两点：<br />1.<strong>对于精度要求不高且任务量较大的场景</strong>：可以适当延长扫描时间间隔，以确保在单次扫描周期内能够完成所有任务的处理下发。这样可以减轻系统负担，提高整体效率。<br />2.<strong>对于精度要求高同时任务量也很大的场景</strong>：除了优化RPC处理流程外，还可以考虑改进数据存储结构，将数据分片分桶处理。通过为每个数据分片分配独立的扫描实例，可以实现并行处理，从而在保证高精度的同时提升系统响应速度。<br />综上所述，合理的扫描时间间隔应当根据具体应用场景和系统需求进行细致调整，以达到最佳的性能和精度平衡点。
<a name="164140b2"></a></p>
<h4 id="状态更新"><strong>状态更新</strong></h4>
<p>为了让我们的系统展现出卓越的性能和高精度，我们采用了异步方式来下发任务。异步处理的明显优势在于它能够使任务并发执行，无需等待响应，从而显著提升了系统的信息处理能力。然而，这也带来了一个问题：我们无法确切知道下游系统是否真正收到了任务。即便上游系统竭尽全力发送任务，如果下游系统接收不到，这些努力也将化为泡影。<br />因此，我们需要下游系统在成功接收到信息后，主动发送一个<strong>确认信号（ACK）</strong>。一旦系统接收到这个ACK，我们就能记录下触发时间和执行时间等相关信息，以便后续的任务重试模块进行相应的处理。<br />考虑到任务是并发下发的，返回的信息量可能会非常庞大，每条返回信息都可能触发一次远程过程调用（RPC），这无疑会大量消耗连接资源。为了解决这个问题，我们引入了队列机制。<br /><img src="/assets/articleSource/2024-08-30-高性能调度/img_2.png" alt="image.png" /><br />队列机制的工作原理如下：<br />通过引入ACK队列，我们实现了以下几个关键效果：<br />1.当任务队列满载时，我们可以一次性取出所有元素，触发一次RPC请求。这样，原本需要多次请求的单个数据处理，现在可以合并为一次批量处理。<br />2.即使任务队列未满，但如果自上次取元素以来经过的时间超过了预设的阈值，我们也会将队列中的所有数据一次性取出，触发一次RPC请求。<br />通过这种方式，我们成功地实现了连接复用和即时响应的双重效果，<strong>这也是一个写聚合的思想。</strong><br />但是坏处也很明显，就是我们这个队列是基于内存的，实例宕机有丢消息的可能性。时间的阈值也需要经验去设置，如果设置短了，连接不会复用，设置长了，可能影响后续任务重试时的扫描，造成误判。<br />这种思想源于Kafka提供的Micro-Batch的概念，他会将相同Topic和Partition的消息聚合成一个批次，然后一次性发送到Kafka集群
<a name="a8b9b9b5"></a></p>
<h4 id="任务重试"><strong>任务重试</strong></h4>
<p>上文我们分析了如何让海量任务下发，但仍然做不到能让调度系统拥有可靠性。在分布式环境下，服务器可能因为网络延迟，服务器故障，资源竞争等原因，任务执行可能会失败。那么如何处理这些失败的任务呢？<br />其实这个问题可以拆解成几个小任务：
<a name="822e66fc"></a></p>
<h5 id="1-如何检测到失败的任务"><strong>1. 如何检测到失败的任务？</strong></h5>
<p>我们上个步骤的下发回流，就是为了收集任务的执行上下文信息。有了这些信息，我们只需要去设置一个定时任务，快速的扫描这个任务信息即可。
<a name="60ce54a3"></a></p>
<h5 id="2-如何定义一个失败的任务"><strong>2. 如何定义一个失败的任务？</strong></h5>
<p>1.在下发一段较长的时间后，仍然没有回流信息写入。<br />2.回流信息写入成功，但回流信息中的响应code为失败。
<a name="d2edc2f5"></a></p>
<h5 id="3-检测到失败任务以后的重试策略"><strong>3. 检测到失败任务以后的重试策略？</strong></h5>
<p>重试策略分为重试次数和重试间隔。<br />每次重试完成，我们需要去更新这个已经重试次数，并检测他是否等于最大重试次数，之所有有这个最大重试次数，是为了防止他无限重试，造成重试风暴，而超过这个最大重试次数的，我们可以把它塞入<strong>死信队列</strong>中，让负责这个任务的人手动的去处理。<br />而重试间隔主要也分为几种：<br /><strong>1. 固定间隔重试</strong><br />在这种策略中，每次重试之间都有一个固定的时间间隔。例如，如果操作失败，系统会在1秒后重试，然后是2秒后重试，依此类推。<br /><strong>2. 指数退避重试</strong><br />指数退避重试策略是一种更复杂的重试策略，其中每次重试之间的时间间隔呈指数增长。例如，第一次重试可能在1秒后，第二次在2秒后，第三次在4秒后，以此类推。这种策略有助于减少对系统的冲击，特别是在高负载或网络拥塞的情况下。<br />之所以采用以上这两种策略是因为rpc接口调用在遇到服务质量异常的错误的时候，由于服务质量异常是有一定时间的，因此有各种退避策略，一定程度上给足下游恢复的时间。
<a name="c678fed7"></a></p>
<h5 id="4-下游应该如何处理重试的任务"><strong>4. 下游应该如何处理重试的任务？</strong></h5>
<p>在扫描的过程中，如果因为网络波动的原因，导致回流消息的时间被拉长，而我们上游在扫描的时候误认为没有下发成功，而实际上已经下发成功了，我们依旧发起了重试，那么就会导致重复下发。<br />为了避免这一现象发生，下游有必要去做一次去重。我们可以给每次下发的任务都冠以一个唯一id，然后用位图对当日的下发进行去重处理。<br />我们可以使用雪花算法去生成唯一id，也可以通过每次生成的业务id去拼接当前下发的秒数去生成唯一id，这个方案很多，不多赘述。</p>

<p><a name="94b65699"></a></p>
<h3 id="路由实例"><strong>路由实例</strong></h3>
<p>在经过上述流程之后，我们需要做的是，选择一个合适的实例进行触发，往往通过<strong>线程池，协程池</strong>进行rpc调度。<br />总结了市面上的开源中间件主要有以下几种路由算法的实现：</p>

<table>
  <thead>
    <tr>
      <th>方法</th>
      <th>描述</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>轮询</td>
      <td>依次遍历执行器列表</td>
    </tr>
    <tr>
      <td>随机数</td>
      <td>random函数实现</td>
    </tr>
    <tr>
      <td>一致性哈希</td>
      <td>通过2^32 ring (md5散列的方式计算hash值)，尽可能保证每轮触发都均匀落到每个执行器上。</td>
    </tr>
    <tr>
      <td>LRU</td>
      <td>最近最久未使用。</td>
    </tr>
    <tr>
      <td>LFU</td>
      <td>每个使用频率最低的执行器优先被淘汰。</td>
    </tr>
    <tr>
      <td>心跳</td>
      <td>遍历每个执行器，向每个执行器发起请求，如果哪个执行器最快发回心跳包，说明他最闲，那么就选择他。</td>
    </tr>
  </tbody>
</table>

<p>这些路由算法都是为了能让不同执行器的负载变得均衡，需要根据场景选择合适的路由算法。</p>

<p><a name="b165504a"></a></p>
<h3 id="优秀系统的设计"><strong>优秀系统的设计</strong></h3>
<p><a name="37c99b53"></a></p>
<h4 id="xxl-job的实现"><strong>xxl-job的实现</strong></h4>
<p>XXL-JOB是一款知名的分布式任务调度框架，它采用内存中的时间轮算法结合MySQL作为持久化存储来管理调度任务，其调度粒度精准至秒级。<br />以下是XXL-JOB的核心工作流程：<br />1.<strong>调度线程预读与更新</strong>：调度线程负责提前读取未来5秒内即将执行的任务，将这些任务载入内存中的时间轮，并同步更新它们的下一次触发时刻。<br />2.<strong>时间轮线程轮询执行：</strong>时间轮线程每隔一秒从等待状态激活为可运行状态。它会捕获当前时间的秒级信息，并在时间轮中定位到对应的任务。一旦找到，时间轮线程便会利用预先配置的线程池发起RPC调用，触发任务执行。<br />下面是简易版的伪代码(笔者凭借之前的印象写的，可能并不完整)：</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 时间轮 秒数为key,task列表为value</span>
<span class="nc">Map</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">,</span><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Task</span><span class="o">&gt;&gt;</span> <span class="n">ringData</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">HashMap</span><span class="o">&lt;&gt;();</span>
<span class="c1">// rpc线程池</span>
<span class="nc">ThreadPoolExecutor</span> <span class="n">threadPool</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ThreadPoolExecutor</span><span class="o">();</span>

<span class="c1">// 调度线程</span>
<span class="nc">Thread</span> <span class="n">schedulerThread</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Thread</span><span class="o">(()</span> <span class="o">-&gt;</span> <span class="o">{</span>
    <span class="k">while</span><span class="o">(</span><span class="kc">true</span><span class="o">){</span>
        <span class="mi">1</span><span class="o">.</span> <span class="n">从数据库预读未来5s的任务</span>
        <span class="nc">List</span><span class="o">&lt;</span><span class="nc">Task</span><span class="o">&gt;</span> <span class="n">tasks</span> <span class="o">=</span> <span class="n">mapper</span><span class="o">.</span><span class="na">loadTask</span><span class="o">(</span><span class="n">now</span><span class="o">()</span> <span class="o">+</span> <span class="mi">5000</span><span class="o">)</span>  
        <span class="k">for</span><span class="o">(</span><span class="n">task</span> <span class="n">in</span> <span class="n">tasks</span><span class="o">){</span>
            <span class="kt">long</span> <span class="n">time</span> <span class="o">=</span> <span class="n">task</span><span class="o">.</span><span class="na">getTriggerTime</span><span class="o">();</span>
            <span class="c1">// 根据触发时间计算秒数。比如触发时间是 21：00：01 那么秒数就是 1</span>
            <span class="nc">Integer</span> <span class="n">second</span> <span class="o">=</span> <span class="n">time</span><span class="o">.</span><span class="na">calculateSecond</span><span class="o">();</span>
            <span class="n">ringData</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="n">task</span><span class="o">.</span><span class="na">getSecond</span><span class="o">(),</span> <span class="n">task</span><span class="o">)</span>
            <span class="c1">// 更新下一次触发事件，运行状态等信息，</span>
            <span class="n">updateTask</span><span class="o">(</span><span class="n">task</span><span class="o">)</span>
        <span class="o">}</span>
        <span class="nc">Thread</span><span class="o">.</span><span class="na">sleep</span><span class="o">(</span><span class="mi">5000</span><span class="o">)</span>
    <span class="o">}</span>
<span class="o">})</span>
<span class="n">schedulerThread</span><span class="o">.</span><span class="na">start</span><span class="o">();</span>

<span class="c1">// 时间轮线程</span>
<span class="nc">Thread</span> <span class="n">ringThread</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Thread</span><span class="o">(()</span> <span class="o">-&gt;</span> <span class="o">{</span>
    <span class="k">while</span><span class="o">(</span><span class="kc">true</span><span class="o">){</span>
       <span class="c1">//秒数对齐，整秒数激活为可运行状态</span>
       <span class="nc">Thread</span><span class="o">.</span><span class="na">sleep</span><span class="o">(</span><span class="mi">1000</span> <span class="o">-</span> <span class="nc">System</span><span class="o">.</span><span class="na">currentTimeMillis</span><span class="o">()</span> <span class="o">%</span> <span class="mi">1000</span><span class="o">);</span>
       <span class="c1">//获取苏醒时候的秒数</span>
       <span class="nc">Integer</span> <span class="n">currentSecond</span> <span class="o">=</span> <span class="n">now</span><span class="o">().</span><span class="na">getSecond</span><span class="o">();</span>
       <span class="nc">List</span><span class="o">&lt;</span><span class="nc">Task</span><span class="o">&gt;</span> <span class="n">tasks</span> <span class="o">=</span> <span class="n">ringData</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="n">currentSecond</span><span class="o">);</span>
       <span class="c1">//放入线程池发起rpc调度</span>
       <span class="n">threadPool</span><span class="o">.</span><span class="na">trigger</span><span class="o">(</span><span class="n">tasks</span><span class="o">)</span>
       <span class="c1">// help gc</span>
       <span class="n">tasks</span><span class="o">.</span><span class="na">clear</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">})</span>
<span class="n">ringThread</span><span class="o">.</span><span class="na">start</span><span class="o">();</span>
</code></pre></div></div>

<p>这里的时间轮就是一个key为秒数，value为即将要执行的任务id列表。<br />时间轮分为单级时间轮和多级时间轮。xxl-job并没有像kafka那样采用多级时间轮，主要是因为设计理念的不同，他为了简化设计，并且单级时间轮已经满足大部分任务调度的需求。<br />优点：<br />1.<strong>高效检索与持久化</strong>：借助MySQL B+树的特性，搜索操作的时间复杂度降至O(logN)，同时提供数据的持久化存储，确保数据安全不易丢失。<br />2.<strong>高精度调度</strong>：通过将任务提前<strong>预读</strong>至内存，提升了任务调度的精度和响应速度。<br />3.<strong>生产者-消费者模型</strong>：调度线程与时间轮线程的协同工作构成了经典的生产者-消费者模型。时间轮作为任务缓冲区，有效实现流量削峰，减轻系统瞬时负载压力。<br />缺点：<br />1.<strong>调度器集群性能瓶颈</strong>：现有架构依赖调度器集群间的锁机制来执行任务，这可能导致单个调度器承受过大压力，限制了系统的并发处理能力（QPS）。在任务量激增时，难以通过水平扩展提升调度能力，从而可能导致部分任务延迟过高。<br />2.<strong>磁盘存储的性能局限</strong>：虽然MySQL提供了可靠的数据存储，但作为基于磁盘的数据库，在高并发场景下的性能可能不如内存型数据库如Redis。<br />总体而言，XXL-JOB采用内存结合MySQL的部署方式简单易行，无需额外引入中间件。这种设计在追求调度精度的同时牺牲了一定的水平扩展性。对于任务量适中的场景而言，它仍然是一个值得考虑的优秀调度框架选项。
<a name="e52a38b0"></a></p>
<h4 id="腾讯视频push中台的实现"><strong>腾讯视频push中台的实现</strong></h4>
<p>腾讯视频push中台为了应对海量的并发，牺牲了调度的精度，以redis作为db，ZSet(跳表)作为底层数据结构来支持任务的范围查询。<br />主要流程：将任务id作为key，时间戳作为value进行任务的新增，利用Zrange 命令获取要触发的任务，并判断是否需要触发。而任务拉取任务依赖不同实例去抢分布式锁，然后执行。<br />优点：<br />1.基于redis，查询快，qps是mysql的好几倍，实现简单，易于维护。<br />2.redis的分布式锁性能优秀，加锁解锁快。<br />3.无需依赖其他中间件，成本小。<br />4.让搜素的时间复杂度降低到O(logN)，查询快，无需遍历额外数据。<br />缺点：精度无法保证。
<a name="eacbc086"></a></p>
<h4 id="redis的高精度版本实现"><strong>Redis的高精度版本实现</strong></h4>
<p><a name="0f9b4069"></a></p>
<h5 id="分片"><strong>分片</strong></h5>
<p>为了实现更高精度的Redis调度，我们需要确保跳表中的数据量保持在合理范围内。过多的数据可能导致内存占用过高、成本不足以及读写响应时间变长等问题(<strong>大Key问题</strong>)。因此，为了降低Redis访问的响应时间（即提高精度），我们对数据进行分片处理，使调度器每次只需扫描一个分片的数据。<br />如下图：<br /><img src="/assets/articleSource/2024-08-30-高性能调度/img_2.png" alt="image.png" /><br />我们可以把一天的数据分为多个分钟级别的数据，虽然搜索的时间复杂度仍为O(logN)，但由于N大大减小，搜索效率得到提高，响应速度更快。<br />然而，这仍然无法解决一个问题：如果某个实例通过抢锁方式获得某一分钟分片的扫描权限，但该分钟内的数据量仍然很大，可能会导致实例的线程数不足，无法实现并发处理。
<a name="b6e85540"></a></p>
<h5 id="分桶"><strong>分桶</strong></h5>
<p>为了解决这个问题，我们可以采用分桶策略，将这一分钟的数据划分为多个bucket。<br />在集群模式的调度器下，每个实例竞争的是各个bucket的锁，获得锁后，只需扫描相应分桶的数据。这种方法可以实现每分钟级别的tasklist调度，多台机器可以同时扫描和下发，避免了单个实例线程不足的问题。<br />如下图：<br /><img src="/assets/articleSource/2024-08-30-高性能调度/img_3.png" alt="image.png" /><br />1<br />若即使分成三个桶，数据量仍然过大，我们可以引入一个决策服务来监控任务的延时情况。如果任务的延时率持续较高，可以根据实际情况<strong>动态调整分桶数量</strong>，从而更好地满足实际需求。</p>

<p><a name="25f9c7fa"></a></p>
<h3 id="总结"><strong>总结</strong></h3>
<p>本文详细探讨了调度模块在多种系统中的应用及其重要性，并深入分析了调度系统的通用流程，包括任务生成、任务存储、定时扫描和路由实例等关键步骤。<br />文章针对每个步骤的技术选型进行了探讨，并结合实际系统（如XXL-JOB和腾讯视频push中台）进行了案例分析。此外，还讨论了各种路由算法的实现及其适用场景。<br />总的来说，一个高性能的调度系统需要综合考虑任务生成策略、存储数据结构的选择、数据库选型、分布式锁的实现以及定时扫描的机制等多个方面。通过合理的技术选型和系统设计，可以实现高精度和高可用的调度目标。同时，根据具体的应用场景和需求，灵活调整调度策略和路由算法，以达到最佳的性能和效率平衡点。</p>

<h2 id="个人问题">个人问题</h2>
<p>个人在读完这篇文章后有一个问题：<br />为什么下发任务走的是RPC而不是MQ？<br />我的理解是这样的：<br />MQ适合的是对消息时效性要求不高的场景。是用来削峰填谷的。消息存在Broker里，消费者可以慢慢消费。<br />但是消息推送系统不一样，它对时效性要求很高。必须保证用户在某个时间点能收到消息推送。扫描任务扫描出的是最近五分钟的推送任务。这些任务必须在短时间内推送到用户手机上。<br />不可能说存到MQ里让消费者慢慢消费。况且走MQ就涉及到了生产者，中间服务器，消费者三个角色，两次数据传输。RPC呢？只有生产者消费者两个角色，数据传输只有一次，在速度上天然就比MQ更快，消费者能更快消费消息。<br />但是缺少了中间服务器，就可能会导致消息积压。<a href="#2e5c6c42">定时扫描</a><br />消费者消费不过来生产者又发来了新的任务怎么办？<br />这就需要更改生产者的生产逻辑。控制扫描任务的时间间隔。<br />怎么控制呢？参考原文：</p>
<blockquote>
  <p>因此，在确定扫描时间间隔时，应考虑以下两点：
1.<strong>对于精度要求不高且任务量较大的场景</strong>：可以适当延长扫描时间间隔，以确保在单次扫描周期内能够完成所有任务的处理下发。这样可以减轻系统负担，提高整体效率。
2.<strong>对于精度要求高同时任务量也很大的场景</strong>：除了优化RPC处理流程外，还可以考虑改进数据存储结构，将数据分片分桶处理。通过为每个数据分片分配独立的扫描实例，可以实现并行处理，从而在保证高精度的同时提升系统响应速度。
综上所述，合理的扫描时间间隔应当根据具体应用场景和系统需求进行细致调整，以达到最佳的性能和精度平衡点。</p>
</blockquote>]]></content><author><name>腾讯技术工程</name></author><category term="系统设计" /><category term="任务调度" /><summary type="html"><![CDATA[更多博客请见 我的语雀知识库 作者：akinwang 调度模块在很多系统中都是常用的模块，比如实习生的每天签到邮件，预约银行的业务短信，学习通的上课通知，腾讯视频push中台的任务下发，调度系统在中间起到关键作用。 原文链接：高性能调度系统设计总结]]></summary></entry><entry><title type="html">ConcurrentHashMap并发扩容</title><link href="/jekyll-theme-yat/java/2024/08/27/concurrentHashMap%E5%B9%B6%E5%8F%91%E6%89%A9%E5%AE%B9.html" rel="alternate" type="text/html" title="ConcurrentHashMap并发扩容" /><published>2024-08-27T00:00:00+00:00</published><updated>2024-08-27T00:00:00+00:00</updated><id>/jekyll-theme-yat/java/2024/08/27/concurrentHashMap%E5%B9%B6%E5%8F%91%E6%89%A9%E5%AE%B9</id><content type="html" xml:base="/jekyll-theme-yat/java/2024/08/27/concurrentHashMap%E5%B9%B6%E5%8F%91%E6%89%A9%E5%AE%B9.html"><![CDATA[<blockquote>
  <p>更多博客请见 <a href="https://www.yuque.com/u41117719/xd1qgc">我的语雀知识库</a><br />
更多细节见<a href="https://javaguide.cn/java/collection/concurrent-hash-map-source-code.html#_3-put">ConcurrentHashMap 源码分析</a></p>
</blockquote>

<p>JDK7和JDK8两个版本的扩容有点不一样。</p>
<h2 id="jdk7">JDK7</h2>
<p>JDK7版本中，扩容是在持有锁时发生的。
<img src="/assets/articleSource/2024-08-27-concurrentHashMap/img_4.png" alt="image.png" />
35行的rehash是扩容函数。直到46行的finally代码块才释放锁。<br />
拿到锁的线程执行put方法时会检查要不要扩容（已有元素个数 / table数组的长度  &gt; 负载因子）。<br />
因此只有一个线程可以执行扩容操作，不存在并发问题。<br />
但是map扩容了总得让其它线程知道吧。具体是怎么知道的呢？<br />
其实数组table是一个volatile变量，这就保证了可见性。<br />
证据见JDK 8中源码778行。我没有装JDK 7,但JDK7里也肯定是一个volatile变量。<br />
<img src="/assets/articleSource/2024-08-27-concurrentHashMap/img.png" alt="image.png" /></p>

<h2 id="jdk8">JDK8</h2>
<p>JDK8版本中，扩容是在不持有锁时发生的。<br />
<img src="/assets/articleSource/2024-08-27-concurrentHashMap/img_1.png" alt="image.png" /><br />
倒数第三行的addCount是扩容的检查函数，可以清晰的看到，它不在syncronized代码块里。</p>

<p>先看看add方法：<br />
<img src="/assets/articleSource/2024-08-27-concurrentHashMap/img_2.png" alt="image.png" /> <br />
<strong>扩容检查流程：</strong></p>

<ul>
  <li>计算元素总量 size，若 CAS 冲突严重则放弃扩容。</li>
  <li>若 size 计算成功，有新元素加入，且检测到元素总量大于阈值 size &gt; sizeCtl。
    <ul>
      <li>如果检查到当前已有线程在进行扩容。
        <ul>
          <li>扩容已经接近完成或足够多的线程参与到扩容中了，当前线程直接返回。</li>
          <li>当前线程参与扩容。</li>
        </ul>
      </li>
      <li>如果没有其他线程在进行扩容，则修改 sizeCtl 标识，进行扩容。</li>
    </ul>
  </li>
</ul>

<h4 id="扩容流程">扩容流程</h4>
<p>上述流程只是扩容之前的准备，扩容的核心逻辑在 transfer() 方法中。</p>
<h5 id="transfer-源码">transfer() 源码</h5>
<p>看源码之前，提前梳理一下扩容过程：</p>

<ol>
  <li>创建 nextTable，新容量是旧容量的 2 倍。</li>
  <li>将原 table 的所有桶逆序分配给多个线程，每个线程每次最小分配 16 个桶，防止资源竞争导致的效率下降。指定范围的桶可能分配给多个线程同时处理。</li>
  <li><strong>扩容时遇到空的桶，采用 CAS 设置为 ForwardingNode 节点，表示该桶扩容完成。</strong></li>
  <li><strong>扩容时遇到 ForwardingNode 节点，表示该桶已扩容过了，直接跳过。</strong></li>
  <li><strong>单个桶内元素的迁移是加锁的</strong>，将旧 table 的 i 位置上所有元素迁移到新表。</li>
  <li>最后将旧 table 的 i 位置设置为 ForwardingNode 节点。</li>
  <li>所有桶扩容完毕，将 table 指向 nextTable，设置 sizeCtl 为新容量 0.75 倍</li>
</ol>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">private</span> <span class="kd">final</span> <span class="kt">void</span> <span class="nf">transfer</span><span class="o">(</span><span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;[]</span> <span class="n">tab</span><span class="o">,</span> <span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;[]</span> <span class="n">nextTab</span><span class="o">)</span> <span class="o">{</span>
    <span class="kt">int</span> <span class="n">n</span> <span class="o">=</span> <span class="n">tab</span><span class="o">.</span><span class="na">length</span><span class="o">,</span> <span class="n">stride</span><span class="o">;</span>
    <span class="k">if</span> <span class="o">((</span><span class="n">stride</span> <span class="o">=</span> <span class="o">(</span><span class="no">NCPU</span> <span class="o">&gt;</span> <span class="mi">1</span><span class="o">)</span> <span class="o">?</span> <span class="o">(</span><span class="n">n</span> <span class="o">&gt;&gt;&gt;</span> <span class="mi">3</span><span class="o">)</span> <span class="o">/</span> <span class="no">NCPU</span> <span class="o">:</span> <span class="n">n</span><span class="o">)</span> <span class="o">&lt;</span> <span class="no">MIN_TRANSFER_STRIDE</span><span class="o">)</span> <span class="c1">// 每核处理的桶的数目，最小为16</span>
        <span class="n">stride</span> <span class="o">=</span> <span class="no">MIN_TRANSFER_STRIDE</span><span class="o">;</span> <span class="c1">// MIN_TRANSFER_STRIDE写死了16，每个线程每次只分配16个桶</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">nextTab</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>            <span class="c1">// initiating</span>
        <span class="k">try</span> <span class="o">{</span>
            <span class="nd">@SuppressWarnings</span><span class="o">(</span><span class="s">"unchecked"</span><span class="o">)</span>
            <span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;[]</span> <span class="n">nt</span> <span class="o">=</span> <span class="o">(</span><span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;[])</span><span class="k">new</span> <span class="nc">Node</span><span class="o">&lt;?,?&gt;[</span><span class="n">n</span> <span class="o">&lt;&lt;</span> <span class="mi">1</span><span class="o">];</span> <span class="c1">// 构建nextTable，其容量为原来容量的两倍</span>
            <span class="n">nextTab</span> <span class="o">=</span> <span class="n">nt</span><span class="o">;</span>
        <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">Throwable</span> <span class="n">ex</span><span class="o">)</span> <span class="o">{</span>      <span class="c1">// try to cope with OOME</span>
            <span class="n">sizeCtl</span> <span class="o">=</span> <span class="nc">Integer</span><span class="o">.</span><span class="na">MAX_VALUE</span><span class="o">;</span>
            <span class="k">return</span><span class="o">;</span>
        <span class="o">}</span>
        <span class="n">nextTable</span> <span class="o">=</span> <span class="n">nextTab</span><span class="o">;</span>
        <span class="n">transferIndex</span> <span class="o">=</span> <span class="n">n</span><span class="o">;</span> <span class="c1">// 迁移总进度，值范围为[0,n]，表示从table的第n-1位开始处理直到第0位。</span>
    <span class="o">}</span>
    <span class="kt">int</span> <span class="n">nextn</span> <span class="o">=</span> <span class="n">nextTab</span><span class="o">.</span><span class="na">length</span><span class="o">;</span>
    <span class="nc">ForwardingNode</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">fwd</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ForwardingNode</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;(</span><span class="n">nextTab</span><span class="o">);</span> <span class="c1">// 扩容时的特殊节点，hash固定为-1，标明此节点正在进行迁移。扩容期间的元素查找要调用其find方法在nextTable中查找元素</span>
    <span class="kt">boolean</span> <span class="n">advance</span> <span class="o">=</span> <span class="kc">true</span><span class="o">;</span> <span class="c1">// 当前线程是否需要继续寻找下一个可处理的节点</span>
    <span class="kt">boolean</span> <span class="n">finishing</span> <span class="o">=</span> <span class="kc">false</span><span class="o">;</span> <span class="c1">// to ensure sweep before committing nextTab // 所有桶是否都已迁移完成</span>
    <span class="k">for</span> <span class="o">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="o">,</span> <span class="n">bound</span> <span class="o">=</span> <span class="mi">0</span><span class="o">;;)</span> <span class="o">{</span>
        <span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">f</span><span class="o">;</span> <span class="kt">int</span> <span class="n">fh</span><span class="o">;</span>
        <span class="k">while</span> <span class="o">(</span><span class="n">advance</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// 此循环的作用是 1.确定当前线程要迁移的桶的范围；2.通过更新i的值确定当前范围内下一个要处理的节点</span>
            <span class="kt">int</span> <span class="n">nextIndex</span><span class="o">,</span> <span class="n">nextBound</span><span class="o">;</span>
            <span class="k">if</span> <span class="o">(--</span><span class="n">i</span> <span class="o">&gt;=</span> <span class="n">bound</span> <span class="o">||</span> <span class="n">finishing</span><span class="o">)</span> <span class="c1">// 每次循环都检查结束条件：i自减没有超过下界，finishing标识为true时，跳出while循环</span>
                <span class="n">advance</span> <span class="o">=</span> <span class="kc">false</span><span class="o">;</span>
            <span class="k">else</span> <span class="nf">if</span> <span class="o">((</span><span class="n">nextIndex</span> <span class="o">=</span> <span class="n">transferIndex</span><span class="o">)</span> <span class="o">&lt;=</span> <span class="mi">0</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// 迁移总进度&lt;=0，表示所有桶都已迁移完成</span>
                <span class="n">i</span> <span class="o">=</span> <span class="o">-</span><span class="mi">1</span><span class="o">;</span>
                <span class="n">advance</span> <span class="o">=</span> <span class="kc">false</span><span class="o">;</span>
            <span class="o">}</span>
                 <span class="cm">/*
                    下面这段代码通过cas的方式确定自己负责的桶范围。
                    假如有多个线程都执行到了下面这行代码，
                    怎么保证只有一个线程可以cas成功？
                    或者说，32-16的桶范围只有一个线程可以拿到？
                    U是一个unsafe对象，可以直接操作内存
                    TRANSFERINDEX通过U这个unsafe对象获取了transferIndex的内存地址，
                    修改TRANSFERINDEX就等于直接修改transferIndex
                    而transferIndex是一个volatile变量
                    每个线程在TRANSFERINDEX=nextIndex时都会发出写屏障修改TRANSFERINDEX
                    但是CPU的总线仲裁机制能够保证：
                    只有一个线程可以发出写屏障更新TRANSFERINDEX(transferIndex)
                    并且向CPU总线发出广播，让其它核心（线程）的缓存块失效
                    等它们从主内存更新缓存块后，cas的条件已经不成立了。于是陷入自旋
                    自旋超时后，会返回false，执行后面的代码。
                    可能这时已经扩容完了没它事。
                    竞争失败后就像个陪读一样走了个过场。
                    我真的没有在说高考
                */</span>
            <span class="k">else</span> <span class="nf">if</span> <span class="o">(</span><span class="no">U</span><span class="o">.</span><span class="na">compareAndSwapInt</span>
                     <span class="o">(</span><span class="k">this</span><span class="o">,</span> <span class="no">TRANSFERINDEX</span><span class="o">,</span> <span class="n">nextIndex</span><span class="o">,</span>
                      <span class="n">nextBound</span> <span class="o">=</span> <span class="o">(</span><span class="n">nextIndex</span> <span class="o">&gt;</span> <span class="n">stride</span> <span class="o">?</span>
                                   <span class="n">nextIndex</span> <span class="o">-</span> <span class="n">stride</span> <span class="o">:</span> <span class="mi">0</span><span class="o">)))</span> <span class="o">{</span> <span class="c1">// CAS执行transferIndex=transferIndex-stride，即transferIndex减去已分配出去的桶，得到边界，这里为下界</span>
                <span class="n">bound</span> <span class="o">=</span> <span class="n">nextBound</span><span class="o">;</span> <span class="c1">// 当前线程需要处理的桶下标的下界</span>
                <span class="n">i</span> <span class="o">=</span> <span class="n">nextIndex</span> <span class="o">-</span> <span class="mi">1</span><span class="o">;</span> <span class="c1">// 当前线程需要处理的桶下标</span>
                <span class="n">advance</span> <span class="o">=</span> <span class="kc">false</span><span class="o">;</span>
            <span class="o">}</span>
        <span class="o">}</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">i</span> <span class="o">&lt;</span> <span class="mi">0</span> <span class="o">||</span> <span class="n">i</span> <span class="o">&gt;=</span> <span class="n">n</span> <span class="o">||</span> <span class="n">i</span> <span class="o">+</span> <span class="n">n</span> <span class="o">&gt;=</span> <span class="n">nextn</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// 当前线程自己的活已经做完或所有线程的活都已做完</span>
            <span class="kt">int</span> <span class="n">sc</span><span class="o">;</span>
            <span class="k">if</span> <span class="o">(</span><span class="n">finishing</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// 已经完成所有节点复制了。所有线程已干完活，最后才走这里</span>
                <span class="n">nextTable</span> <span class="o">=</span> <span class="kc">null</span><span class="o">;</span>
                <span class="n">table</span> <span class="o">=</span> <span class="n">nextTab</span><span class="o">;</span> <span class="c1">// table指向nextTable</span>
                <span class="n">sizeCtl</span> <span class="o">=</span> <span class="o">(</span><span class="n">n</span> <span class="o">&lt;&lt;</span> <span class="mi">1</span><span class="o">)</span> <span class="o">-</span> <span class="o">(</span><span class="n">n</span> <span class="o">&gt;&gt;&gt;</span> <span class="mi">1</span><span class="o">);</span> <span class="c1">// 设置sizeCtl为新容量0.75倍</span>
                <span class="k">return</span><span class="o">;</span>
            <span class="o">}</span>
            <span class="k">if</span> <span class="o">(</span><span class="no">U</span><span class="o">.</span><span class="na">compareAndSwapInt</span><span class="o">(</span><span class="k">this</span><span class="o">,</span> <span class="no">SIZECTL</span><span class="o">,</span> <span class="n">sc</span> <span class="o">=</span> <span class="n">sizeCtl</span><span class="o">,</span> <span class="n">sc</span> <span class="o">-</span> <span class="mi">1</span><span class="o">))</span> <span class="o">{</span> <span class="c1">// 当前线程已结束扩容，sizeCtl-1表示参与扩容线程数-1</span>
                <span class="k">if</span> <span class="o">((</span><span class="n">sc</span> <span class="o">-</span> <span class="mi">2</span><span class="o">)</span> <span class="o">!=</span> <span class="n">resizeStamp</span><span class="o">(</span><span class="n">n</span><span class="o">)</span> <span class="o">&lt;&lt;</span> <span class="no">RESIZE_STAMP_SHIFT</span><span class="o">)</span> <span class="c1">// 相等时说明没有线程在参与扩容了，置finishing=advance=true，为保险让i=n再检查一次</span>
                    <span class="k">return</span><span class="o">;</span>
                <span class="n">finishing</span> <span class="o">=</span> <span class="n">advance</span> <span class="o">=</span> <span class="kc">true</span><span class="o">;</span>
                <span class="n">i</span> <span class="o">=</span> <span class="n">n</span><span class="o">;</span> <span class="c1">// recheck before commit</span>
            <span class="o">}</span>
        <span class="o">}</span>
        <span class="k">else</span> <span class="nf">if</span> <span class="o">((</span><span class="n">f</span> <span class="o">=</span> <span class="n">tabAt</span><span class="o">(</span><span class="n">tab</span><span class="o">,</span> <span class="n">i</span><span class="o">))</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="c1">// 遍历到i位置为null，则放入ForwardingNode节点，标志该桶扩容完成。</span>
            <span class="n">advance</span> <span class="o">=</span> <span class="n">casTabAt</span><span class="o">(</span><span class="n">tab</span><span class="o">,</span> <span class="n">i</span><span class="o">,</span> <span class="kc">null</span><span class="o">,</span> <span class="n">fwd</span><span class="o">);</span>
        <span class="k">else</span> <span class="nf">if</span> <span class="o">((</span><span class="n">fh</span> <span class="o">=</span> <span class="n">f</span><span class="o">.</span><span class="na">hash</span><span class="o">)</span> <span class="o">==</span> <span class="no">MOVED</span><span class="o">)</span> <span class="c1">// f.hash == -1 表示遍历到了ForwardingNode节点，意味着该节点已经处理过了</span>
            <span class="n">advance</span> <span class="o">=</span> <span class="kc">true</span><span class="o">;</span> <span class="c1">// already processed</span>
        <span class="k">else</span> <span class="o">{</span>
            <span class="kd">synchronized</span> <span class="o">(</span><span class="n">f</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// 桶内元素迁移需要加锁</span>
                <span class="k">if</span> <span class="o">(</span><span class="n">tabAt</span><span class="o">(</span><span class="n">tab</span><span class="o">,</span> <span class="n">i</span><span class="o">)</span> <span class="o">==</span> <span class="n">f</span><span class="o">)</span> <span class="o">{</span>
                    <span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">ln</span><span class="o">,</span> <span class="n">hn</span><span class="o">;</span>
                    <span class="k">if</span> <span class="o">(</span><span class="n">fh</span> <span class="o">&gt;=</span> <span class="mi">0</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// 链表节点。非链表节点hash值小于0</span>
                        <span class="kt">int</span> <span class="n">runBit</span> <span class="o">=</span> <span class="n">fh</span> <span class="o">&amp;</span> <span class="n">n</span><span class="o">;</span> <span class="c1">// 根据 hash&amp;n 的结果，将所有结点分为两部分</span>
                        <span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">lastRun</span> <span class="o">=</span> <span class="n">f</span><span class="o">;</span>
                        <span class="k">for</span> <span class="o">(</span><span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">p</span> <span class="o">=</span> <span class="n">f</span><span class="o">.</span><span class="na">next</span><span class="o">;</span> <span class="n">p</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">;</span> <span class="n">p</span> <span class="o">=</span> <span class="n">p</span><span class="o">.</span><span class="na">next</span><span class="o">)</span> <span class="o">{</span>
                            <span class="kt">int</span> <span class="n">b</span> <span class="o">=</span> <span class="n">p</span><span class="o">.</span><span class="na">hash</span> <span class="o">&amp;</span> <span class="n">n</span><span class="o">;</span> <span class="c1">// 遍历链表的每个节点，依次计算 hash&amp;n</span>
                            <span class="k">if</span> <span class="o">(</span><span class="n">b</span> <span class="o">!=</span> <span class="n">runBit</span><span class="o">)</span> <span class="o">{</span>
                                <span class="n">runBit</span> <span class="o">=</span> <span class="n">b</span><span class="o">;</span>
                                <span class="n">lastRun</span> <span class="o">=</span> <span class="n">p</span><span class="o">;</span>
                            <span class="o">}</span>
                        <span class="o">}</span>
                        <span class="k">if</span> <span class="o">(</span><span class="n">runBit</span> <span class="o">==</span> <span class="mi">0</span><span class="o">)</span> <span class="o">{</span>
                            <span class="n">ln</span> <span class="o">=</span> <span class="n">lastRun</span><span class="o">;</span>
                            <span class="n">hn</span> <span class="o">=</span> <span class="kc">null</span><span class="o">;</span>
                        <span class="o">}</span>
                        <span class="k">else</span> <span class="o">{</span>
                            <span class="n">hn</span> <span class="o">=</span> <span class="n">lastRun</span><span class="o">;</span>
                            <span class="n">ln</span> <span class="o">=</span> <span class="kc">null</span><span class="o">;</span>
                        <span class="o">}</span>
                        <span class="k">for</span> <span class="o">(</span><span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">p</span> <span class="o">=</span> <span class="n">f</span><span class="o">;</span> <span class="n">p</span> <span class="o">!=</span> <span class="n">lastRun</span><span class="o">;</span> <span class="n">p</span> <span class="o">=</span> <span class="n">p</span><span class="o">.</span><span class="na">next</span><span class="o">)</span> <span class="o">{</span>
                            <span class="kt">int</span> <span class="n">ph</span> <span class="o">=</span> <span class="n">p</span><span class="o">.</span><span class="na">hash</span><span class="o">;</span> <span class="no">K</span> <span class="n">pk</span> <span class="o">=</span> <span class="n">p</span><span class="o">.</span><span class="na">key</span><span class="o">;</span> <span class="no">V</span> <span class="n">pv</span> <span class="o">=</span> <span class="n">p</span><span class="o">.</span><span class="na">val</span><span class="o">;</span>
                            <span class="k">if</span> <span class="o">((</span><span class="n">ph</span> <span class="o">&amp;</span> <span class="n">n</span><span class="o">)</span> <span class="o">==</span> <span class="mi">0</span><span class="o">)</span>
                                <span class="n">ln</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;(</span><span class="n">ph</span><span class="o">,</span> <span class="n">pk</span><span class="o">,</span> <span class="n">pv</span><span class="o">,</span> <span class="n">ln</span><span class="o">);</span> <span class="c1">// hash&amp;n为0，索引位置不变，作低位链表</span>
                            <span class="k">else</span>
                                <span class="n">hn</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;(</span><span class="n">ph</span><span class="o">,</span> <span class="n">pk</span><span class="o">,</span> <span class="n">pv</span><span class="o">,</span> <span class="n">hn</span><span class="o">);</span> <span class="c1">// hash&amp;n不为0，索引变成“原索引+oldCap”，作高位链表</span>
                        <span class="o">}</span>
                        <span class="n">setTabAt</span><span class="o">(</span><span class="n">nextTab</span><span class="o">,</span> <span class="n">i</span><span class="o">,</span> <span class="n">ln</span><span class="o">);</span> <span class="c1">// 低位链表放在i处</span>
                        <span class="n">setTabAt</span><span class="o">(</span><span class="n">nextTab</span><span class="o">,</span> <span class="n">i</span> <span class="o">+</span> <span class="n">n</span><span class="o">,</span> <span class="n">hn</span><span class="o">);</span> <span class="c1">// 高位链表放在i+n处</span>
                        <span class="n">setTabAt</span><span class="o">(</span><span class="n">tab</span><span class="o">,</span> <span class="n">i</span><span class="o">,</span> <span class="n">fwd</span><span class="o">);</span> <span class="c1">// 在原table的i位置设置ForwardingNode节点，以提示该桶扩容完成</span>
                        <span class="n">advance</span> <span class="o">=</span> <span class="kc">true</span><span class="o">;</span>
                    <span class="o">}</span>
                    <span class="k">else</span> <span class="nf">if</span> <span class="o">(</span><span class="n">f</span> <span class="k">instanceof</span> <span class="nc">TreeBin</span><span class="o">)</span> <span class="o">{</span>
                        <span class="nc">TreeBin</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">t</span> <span class="o">=</span> <span class="o">(</span><span class="nc">TreeBin</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;)</span><span class="n">f</span><span class="o">;</span>
                        <span class="nc">TreeNode</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">lo</span> <span class="o">=</span> <span class="kc">null</span><span class="o">,</span> <span class="n">loTail</span> <span class="o">=</span> <span class="kc">null</span><span class="o">;</span>
                        <span class="nc">TreeNode</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">hi</span> <span class="o">=</span> <span class="kc">null</span><span class="o">,</span> <span class="n">hiTail</span> <span class="o">=</span> <span class="kc">null</span><span class="o">;</span>
                        <span class="kt">int</span> <span class="n">lc</span> <span class="o">=</span> <span class="mi">0</span><span class="o">,</span> <span class="n">hc</span> <span class="o">=</span> <span class="mi">0</span><span class="o">;</span>
                        <span class="k">for</span> <span class="o">(</span><span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">e</span> <span class="o">=</span> <span class="n">t</span><span class="o">.</span><span class="na">first</span><span class="o">;</span> <span class="n">e</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">;</span> <span class="n">e</span> <span class="o">=</span> <span class="n">e</span><span class="o">.</span><span class="na">next</span><span class="o">)</span> <span class="o">{</span>
                            <span class="kt">int</span> <span class="n">h</span> <span class="o">=</span> <span class="n">e</span><span class="o">.</span><span class="na">hash</span><span class="o">;</span>
                            <span class="nc">TreeNode</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">p</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">TreeNode</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span>
                                <span class="o">(</span><span class="n">h</span><span class="o">,</span> <span class="n">e</span><span class="o">.</span><span class="na">key</span><span class="o">,</span> <span class="n">e</span><span class="o">.</span><span class="na">val</span><span class="o">,</span> <span class="kc">null</span><span class="o">,</span> <span class="kc">null</span><span class="o">);</span>
                            <span class="k">if</span> <span class="o">((</span><span class="n">h</span> <span class="o">&amp;</span> <span class="n">n</span><span class="o">)</span> <span class="o">==</span> <span class="mi">0</span><span class="o">)</span> <span class="o">{</span>
                                <span class="k">if</span> <span class="o">((</span><span class="n">p</span><span class="o">.</span><span class="na">prev</span> <span class="o">=</span> <span class="n">loTail</span><span class="o">)</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span>
                                    <span class="n">lo</span> <span class="o">=</span> <span class="n">p</span><span class="o">;</span>
                                <span class="k">else</span>
                                    <span class="n">loTail</span><span class="o">.</span><span class="na">next</span> <span class="o">=</span> <span class="n">p</span><span class="o">;</span>
                                <span class="n">loTail</span> <span class="o">=</span> <span class="n">p</span><span class="o">;</span>
                                <span class="o">++</span><span class="n">lc</span><span class="o">;</span>
                            <span class="o">}</span>
                            <span class="k">else</span> <span class="o">{</span>
                                <span class="k">if</span> <span class="o">((</span><span class="n">p</span><span class="o">.</span><span class="na">prev</span> <span class="o">=</span> <span class="n">hiTail</span><span class="o">)</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span>
                                    <span class="n">hi</span> <span class="o">=</span> <span class="n">p</span><span class="o">;</span>
                                <span class="k">else</span>
                                    <span class="n">hiTail</span><span class="o">.</span><span class="na">next</span> <span class="o">=</span> <span class="n">p</span><span class="o">;</span>
                                <span class="n">hiTail</span> <span class="o">=</span> <span class="n">p</span><span class="o">;</span>
                                <span class="o">++</span><span class="n">hc</span><span class="o">;</span>
                            <span class="o">}</span>
                        <span class="o">}</span>
                        <span class="n">ln</span> <span class="o">=</span> <span class="o">(</span><span class="n">lc</span> <span class="o">&lt;=</span> <span class="no">UNTREEIFY_THRESHOLD</span><span class="o">)</span> <span class="o">?</span> <span class="n">untreeify</span><span class="o">(</span><span class="n">lo</span><span class="o">)</span> <span class="o">:</span>
                            <span class="o">(</span><span class="n">hc</span> <span class="o">!=</span> <span class="mi">0</span><span class="o">)</span> <span class="o">?</span> <span class="k">new</span> <span class="nc">TreeBin</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;(</span><span class="n">lo</span><span class="o">)</span> <span class="o">:</span> <span class="n">t</span><span class="o">;</span>
                        <span class="n">hn</span> <span class="o">=</span> <span class="o">(</span><span class="n">hc</span> <span class="o">&lt;=</span> <span class="no">UNTREEIFY_THRESHOLD</span><span class="o">)</span> <span class="o">?</span> <span class="n">untreeify</span><span class="o">(</span><span class="n">hi</span><span class="o">)</span> <span class="o">:</span>
                            <span class="o">(</span><span class="n">lc</span> <span class="o">!=</span> <span class="mi">0</span><span class="o">)</span> <span class="o">?</span> <span class="k">new</span> <span class="nc">TreeBin</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;(</span><span class="n">hi</span><span class="o">)</span> <span class="o">:</span> <span class="n">t</span><span class="o">;</span>
                        <span class="n">setTabAt</span><span class="o">(</span><span class="n">nextTab</span><span class="o">,</span> <span class="n">i</span><span class="o">,</span> <span class="n">ln</span><span class="o">);</span>
                        <span class="n">setTabAt</span><span class="o">(</span><span class="n">nextTab</span><span class="o">,</span> <span class="n">i</span> <span class="o">+</span> <span class="n">n</span><span class="o">,</span> <span class="n">hn</span><span class="o">);</span>
                        <span class="n">setTabAt</span><span class="o">(</span><span class="n">tab</span><span class="o">,</span> <span class="n">i</span><span class="o">,</span> <span class="n">fwd</span><span class="o">);</span>
                        <span class="n">advance</span> <span class="o">=</span> <span class="kc">true</span><span class="o">;</span>
                    <span class="o">}</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h5 id="一些并发问题">一些并发问题：</h5>
<h6 id="怎么保证一个桶范围和一个线程一一对应">怎么保证一个桶范围和一个线程一一对应？</h6>
<p>有一个全局的volatile变量transferIndex。它记录最新的哈希桶迁移索引。<br />
每个线程通过cas的方式获取自己的哈希桶范围。如果有多个线程预期的哈希桶范围都一样，<br />
那么因为cas的原因，只有一个线程可以拥有这段范围并且更新transferIndex的值。其它线程自旋等待，超时才进入下一步<br />
具体见下面这段解释：</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">f</span><span class="o">;</span> <span class="kt">int</span> <span class="n">fh</span><span class="o">;</span>
        <span class="k">while</span> <span class="o">(</span><span class="n">advance</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// 此循环的作用是 1.确定当前线程要迁移的桶的范围；2.通过更新i的值确定当前范围内下一个要处理的节点</span>
            <span class="kt">int</span> <span class="n">nextIndex</span><span class="o">,</span> <span class="n">nextBound</span><span class="o">;</span>
            <span class="k">if</span> <span class="o">(--</span><span class="n">i</span> <span class="o">&gt;=</span> <span class="n">bound</span> <span class="o">||</span> <span class="n">finishing</span><span class="o">)</span> <span class="c1">// 每次循环都检查结束条件：i自减没有超过下界，finishing标识为true时，跳出while循环</span>
                <span class="n">advance</span> <span class="o">=</span> <span class="kc">false</span><span class="o">;</span>
            <span class="k">else</span> <span class="nf">if</span> <span class="o">((</span><span class="n">nextIndex</span> <span class="o">=</span> <span class="n">transferIndex</span><span class="o">)</span> <span class="o">&lt;=</span> <span class="mi">0</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// 迁移总进度&lt;=0，表示所有桶都已迁移完成</span>
                <span class="n">i</span> <span class="o">=</span> <span class="o">-</span><span class="mi">1</span><span class="o">;</span>
                <span class="n">advance</span> <span class="o">=</span> <span class="kc">false</span><span class="o">;</span>
            <span class="o">}</span>
                <span class="cm">/*
                    下面这段代码通过cas的方式确定自己负责的桶范围。
                    假如有多个线程都执行到了下面这行代码，
                    怎么保证只有一个线程可以cas成功？或者说，32-16的桶范围只有一个线程可以拿到？
                    U是一个unsafe对象，可以直接操作内存
                    TRANSFERINDEX通过U这个unsafe对象获取了transferIndex的内存地址，
                    修改TRANSFERINDEX就等于直接修改transferIndex
                    而transferIndex是一个volatile变量
                    每个线程在TRANSFERINDEX = nextIndex时都会请求发出写屏障修改TRANSFERINDEX
                    但是CPU的总线仲裁机制能够保证：
                    只有一个线程可以发出写屏障更新TRANSFERINDEX(transferIndex)
                    并且向CPU总线发出广播，让其它线程的缓存块失效
                    等它们从主内存更新缓存块后，cas的条件已经不成立了。于是陷入自旋
                    自旋超时后，会返回false，执行后面的代码。
                    可能这时已经扩容完了没它事。
                    竞争失败后就像个陪读一样走了个过场。
                    我真的没有在说高考
                */</span>
            <span class="k">else</span> <span class="nf">if</span> <span class="o">(</span><span class="no">U</span><span class="o">.</span><span class="na">compareAndSwapInt</span>
                     <span class="o">(</span><span class="k">this</span><span class="o">,</span> <span class="no">TRANSFERINDEX</span><span class="o">,</span> <span class="n">nextIndex</span><span class="o">,</span>
                      <span class="n">nextBound</span> <span class="o">=</span> <span class="o">(</span><span class="n">nextIndex</span> <span class="o">&gt;</span> <span class="n">stride</span> <span class="o">?</span>
                                   <span class="n">nextIndex</span> <span class="o">-</span> <span class="n">stride</span> <span class="o">:</span> <span class="mi">0</span><span class="o">)))</span> <span class="o">{</span> <span class="c1">// CAS执行transferIndex=transferIndex-stride，即transferIndex减去已分配出去的桶，得到边界，这里为下界</span>
                <span class="n">bound</span> <span class="o">=</span> <span class="n">nextBound</span><span class="o">;</span> <span class="c1">// 当前线程需要处理的桶下标的下界</span>
                <span class="n">i</span> <span class="o">=</span> <span class="n">nextIndex</span> <span class="o">-</span> <span class="mi">1</span><span class="o">;</span> <span class="c1">// 当前线程需要处理的桶下标</span>
                <span class="n">advance</span> <span class="o">=</span> <span class="kc">false</span><span class="o">;</span>
            <span class="o">}</span>
        <span class="o">}</span>
</code></pre></div></div>

<p>TRANSFERINDEX在源码的6357行通过unsafe对象获取了transferIndex的内存地址<br />
<img src="/assets/articleSource/2024-08-27-concurrentHashMap/img_3.png" alt="image.png" /></p>]]></content><author><name>xyhao</name></author><category term="Java" /><category term="Java" /><category term="HashMap" /><summary type="html"><![CDATA[更多博客请见 我的语雀知识库 更多细节见ConcurrentHashMap 源码分析]]></summary></entry><entry><title type="html">HashMap线程不安全举例</title><link href="/jekyll-theme-yat/hashmap/2024/08/21/HashMap%E7%BA%BF%E7%A8%8B%E4%B8%8D%E5%AE%89%E5%85%A8%E4%B8%BE%E4%BE%8B.html" rel="alternate" type="text/html" title="HashMap线程不安全举例" /><published>2024-08-21T00:00:00+00:00</published><updated>2024-08-21T00:00:00+00:00</updated><id>/jekyll-theme-yat/hashmap/2024/08/21/HashMap%E7%BA%BF%E7%A8%8B%E4%B8%8D%E5%AE%89%E5%85%A8%E4%B8%BE%E4%BE%8B</id><content type="html" xml:base="/jekyll-theme-yat/hashmap/2024/08/21/HashMap%E7%BA%BF%E7%A8%8B%E4%B8%8D%E5%AE%89%E5%85%A8%E4%B8%BE%E4%BE%8B.html"><![CDATA[<blockquote>
  <p>更多博客请见 <a href="https://www.yuque.com/u41117719/xd1qgc">我的语雀知识库</a></p>
</blockquote>

<h2 id="链表成环">链表成环</h2>
<p>这个问题是JDK7中put方法是头插法导致的<br />
JDK8及后续版本已经改成尾插法修复此问题<br />
<a href="https://www.yuque.com/u41117719/xd1qgc/heysdsfv6flmxuag">哈希表成环</a></p>
<h2 id="数据丢失">数据丢失</h2>
<p>共有两种数据丢失的位置：</p>
<ol>
  <li>
    <p>table[i]为null时的哈希桶头部<br />
有两个线程：B插入（2，”two”）,A插入（1，”one”）。<br />
假如table[15]这个哈希桶是null，A和B并发执行中都知道15是null<br />
B先执行，tab[15] = new node(hash, 2,”two”, null)<br />
A后执行，tab[15] = new node(hash, 1,”one”, null)<br />
正常来说应该是<br />
  tab[15] = new node(hash, 2,”two”, null)<br />
  tab[15].next = new node(hash, 1, “one”, null)<br />
  但实际是<br />
  tab[15] = new node(hash, 1, “one”, null)  <br />
  tab[15].next = null<br />
但是A把B put 的数据覆盖了。get(2)是get不到的</p>
  </li>
  <li>
    <p>table[i]不为null时的哈希桶尾部<br />
看下面源码就知道其实和前一种情况是一样的</p>
  </li>
</ol>

<p>结合源码看看：</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">final</span> <span class="no">V</span> <span class="nf">putVal</span><span class="o">(</span><span class="kt">int</span> <span class="n">hash</span><span class="o">,</span> <span class="no">K</span> <span class="n">key</span><span class="o">,</span> <span class="no">V</span> <span class="n">value</span><span class="o">,</span> <span class="kt">boolean</span> <span class="n">onlyIfAbsent</span><span class="o">,</span>
                   <span class="kt">boolean</span> <span class="n">evict</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;[]</span> <span class="n">tab</span><span class="o">;</span> <span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">p</span><span class="o">;</span> <span class="kt">int</span> <span class="n">n</span><span class="o">,</span> <span class="n">i</span><span class="o">;</span>
        <span class="k">if</span> <span class="o">((</span><span class="n">tab</span> <span class="o">=</span> <span class="n">table</span><span class="o">)</span> <span class="o">==</span> <span class="kc">null</span> <span class="o">||</span> <span class="o">(</span><span class="n">n</span> <span class="o">=</span> <span class="n">tab</span><span class="o">.</span><span class="na">length</span><span class="o">)</span> <span class="o">==</span> <span class="mi">0</span><span class="o">)</span>
            <span class="n">n</span> <span class="o">=</span> <span class="o">(</span><span class="n">tab</span> <span class="o">=</span> <span class="n">resize</span><span class="o">()).</span><span class="na">length</span><span class="o">;</span>
        
        <span class="k">if</span> <span class="o">((</span><span class="n">p</span> <span class="o">=</span> <span class="n">tab</span><span class="o">[</span><span class="n">i</span> <span class="o">=</span> <span class="o">(</span><span class="n">n</span> <span class="o">-</span> <span class="mi">1</span><span class="o">)</span> <span class="o">&amp;</span> <span class="n">hash</span><span class="o">])</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="c1">// A和B都运行到这里，但是A时间片用尽，B先执行</span>
            <span class="cm">/*
                table[i]为null时数据丢失问题
                B先执行，tab[15] = new node(hash, 2,"two", null);
                A后执行，tab[15] = new node(hash, 1,"one", null);
                完美把B覆盖了
                这里的并发问题是加锁 &amp;&amp; 加判断解决的
            */</span>
            <span class="n">tab</span><span class="o">[</span><span class="n">i</span><span class="o">]</span> <span class="o">=</span> <span class="n">newNode</span><span class="o">(</span><span class="n">hash</span><span class="o">,</span> <span class="n">key</span><span class="o">,</span> <span class="n">value</span><span class="o">,</span> <span class="kc">null</span><span class="o">);</span> 
        <span class="k">else</span> <span class="o">{</span>
            <span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">e</span><span class="o">;</span> <span class="no">K</span> <span class="n">k</span><span class="o">;</span>
            <span class="k">if</span> <span class="o">(</span><span class="n">p</span><span class="o">.</span><span class="na">hash</span> <span class="o">==</span> <span class="n">hash</span> <span class="o">&amp;&amp;</span>
                <span class="o">((</span><span class="n">k</span> <span class="o">=</span> <span class="n">p</span><span class="o">.</span><span class="na">key</span><span class="o">)</span> <span class="o">==</span> <span class="n">key</span> <span class="o">||</span> <span class="o">(</span><span class="n">key</span> <span class="o">!=</span> <span class="kc">null</span> <span class="o">&amp;&amp;</span> <span class="n">key</span><span class="o">.</span><span class="na">equals</span><span class="o">(</span><span class="n">k</span><span class="o">))))</span>
                <span class="n">e</span> <span class="o">=</span> <span class="n">p</span><span class="o">;</span>
            <span class="k">else</span> <span class="nf">if</span> <span class="o">(</span><span class="n">p</span> <span class="k">instanceof</span> <span class="nc">TreeNode</span><span class="o">)</span>
                <span class="n">e</span> <span class="o">=</span> <span class="o">((</span><span class="nc">TreeNode</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;)</span><span class="n">p</span><span class="o">).</span><span class="na">putTreeVal</span><span class="o">(</span><span class="k">this</span><span class="o">,</span> <span class="n">tab</span><span class="o">,</span> <span class="n">hash</span><span class="o">,</span> <span class="n">key</span><span class="o">,</span> <span class="n">value</span><span class="o">);</span>
            <span class="k">else</span> <span class="o">{</span>
                <span class="k">for</span> <span class="o">(</span><span class="kt">int</span> <span class="n">binCount</span> <span class="o">=</span> <span class="mi">0</span><span class="o">;</span> <span class="o">;</span> <span class="o">++</span><span class="n">binCount</span><span class="o">)</span> <span class="o">{</span>
                    <span class="c1">//下面这行也会出现并发问题，这个并发问题加锁直接解决了</span>
                    <span class="k">if</span> <span class="o">((</span><span class="n">e</span> <span class="o">=</span> <span class="n">p</span><span class="o">.</span><span class="na">next</span><span class="o">)</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
                         <span class="cm">/*
                            table[i]不为null时数据丢失问题
                            B先执行，p.next = new node(hash, 2,"two", null);
                            A后执行，p.next = new node(hash, 1,"one", null);
                            完美把B覆盖了
                        */</span>
                        <span class="n">p</span><span class="o">.</span><span class="na">next</span> <span class="o">=</span> <span class="n">newNode</span><span class="o">(</span><span class="n">hash</span><span class="o">,</span> <span class="n">key</span><span class="o">,</span> <span class="n">value</span><span class="o">,</span> <span class="kc">null</span><span class="o">);</span>
                        <span class="k">if</span> <span class="o">(</span><span class="n">binCount</span> <span class="o">&gt;=</span> <span class="no">TREEIFY_THRESHOLD</span> <span class="o">-</span> <span class="mi">1</span><span class="o">)</span> <span class="c1">// -1 for 1st</span>
                            <span class="n">treeifyBin</span><span class="o">(</span><span class="n">tab</span><span class="o">,</span> <span class="n">hash</span><span class="o">);</span>
                        <span class="k">break</span><span class="o">;</span>
                    <span class="o">}</span>
                    <span class="k">if</span> <span class="o">(</span><span class="n">e</span><span class="o">.</span><span class="na">hash</span> <span class="o">==</span> <span class="n">hash</span> <span class="o">&amp;&amp;</span>
                        <span class="o">((</span><span class="n">k</span> <span class="o">=</span> <span class="n">e</span><span class="o">.</span><span class="na">key</span><span class="o">)</span> <span class="o">==</span> <span class="n">key</span> <span class="o">||</span> <span class="o">(</span><span class="n">key</span> <span class="o">!=</span> <span class="kc">null</span> <span class="o">&amp;&amp;</span> <span class="n">key</span><span class="o">.</span><span class="na">equals</span><span class="o">(</span><span class="n">k</span><span class="o">))))</span>
                        <span class="k">break</span><span class="o">;</span>
                    <span class="n">p</span> <span class="o">=</span> <span class="n">e</span><span class="o">;</span>
                <span class="o">}</span>
            <span class="o">}</span>
            <span class="k">if</span> <span class="o">(</span><span class="n">e</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// existing mapping for key</span>
                <span class="no">V</span> <span class="n">oldValue</span> <span class="o">=</span> <span class="n">e</span><span class="o">.</span><span class="na">value</span><span class="o">;</span>
                <span class="k">if</span> <span class="o">(!</span><span class="n">onlyIfAbsent</span> <span class="o">||</span> <span class="n">oldValue</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span>
                    <span class="n">e</span><span class="o">.</span><span class="na">value</span> <span class="o">=</span> <span class="n">value</span><span class="o">;</span>
                <span class="n">afterNodeAccess</span><span class="o">(</span><span class="n">e</span><span class="o">);</span>
                <span class="k">return</span> <span class="n">oldValue</span><span class="o">;</span>
            <span class="o">}</span>
        <span class="o">}</span>
        <span class="o">++</span><span class="n">modCount</span><span class="o">;</span>
        <span class="k">if</span> <span class="o">(++</span><span class="n">size</span> <span class="o">&gt;</span> <span class="n">threshold</span><span class="o">)</span>
            <span class="n">resize</span><span class="o">();</span>
        <span class="n">afterNodeInsertion</span><span class="o">(</span><span class="n">evict</span><span class="o">);</span>
        <span class="k">return</span> <span class="kc">null</span><span class="o">;</span>
    <span class="o">}</span>
</code></pre></div></div>

<h2 id="解决方案">解决方案</h2>
<p>如何解决？<br />
加锁。<br />
看看ConcurrentHashMap的源码是怎么做的（JDK21）</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="no">V</span> <span class="nf">put</span><span class="o">(</span><span class="no">K</span> <span class="n">key</span><span class="o">,</span> <span class="no">V</span> <span class="n">value</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="nf">putVal</span><span class="o">(</span><span class="n">key</span><span class="o">,</span> <span class="n">value</span><span class="o">,</span> <span class="kc">false</span><span class="o">);</span>
    <span class="o">}</span>

<span class="kd">final</span> <span class="no">V</span> <span class="nf">putVal</span><span class="o">(</span><span class="no">K</span> <span class="n">key</span><span class="o">,</span> <span class="no">V</span> <span class="n">value</span><span class="o">,</span> <span class="kt">boolean</span> <span class="n">onlyIfAbsent</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">key</span> <span class="o">==</span> <span class="kc">null</span> <span class="o">||</span> <span class="n">value</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="k">throw</span> <span class="k">new</span> <span class="nc">NullPointerException</span><span class="o">();</span>
        <span class="kt">int</span> <span class="n">hash</span> <span class="o">=</span> <span class="n">spread</span><span class="o">(</span><span class="n">key</span><span class="o">.</span><span class="na">hashCode</span><span class="o">());</span>
        <span class="kt">int</span> <span class="n">binCount</span> <span class="o">=</span> <span class="mi">0</span><span class="o">;</span>
        <span class="k">for</span> <span class="o">(</span><span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;[]</span> <span class="n">tab</span> <span class="o">=</span> <span class="n">table</span><span class="o">;;)</span> <span class="o">{</span>
            <span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">f</span><span class="o">;</span> <span class="kt">int</span> <span class="n">n</span><span class="o">,</span> <span class="n">i</span><span class="o">,</span> <span class="n">fh</span><span class="o">;</span> <span class="no">K</span> <span class="n">fk</span><span class="o">;</span> <span class="no">V</span> <span class="n">fv</span><span class="o">;</span>
            
            <span class="c1">// 整个表都为null</span>
            <span class="k">if</span> <span class="o">(</span><span class="n">tab</span> <span class="o">==</span> <span class="kc">null</span> <span class="o">||</span> <span class="o">(</span><span class="n">n</span> <span class="o">=</span> <span class="n">tab</span><span class="o">.</span><span class="na">length</span><span class="o">)</span> <span class="o">==</span> <span class="mi">0</span><span class="o">)</span>
                <span class="n">tab</span> <span class="o">=</span> <span class="n">initTable</span><span class="o">();</span>
                
            <span class="c1">// 要插入的桶为null，就通过cas插入</span>
            <span class="k">else</span> <span class="nf">if</span> <span class="o">((</span><span class="n">f</span> <span class="o">=</span> <span class="n">tabAt</span><span class="o">(</span><span class="n">tab</span><span class="o">,</span> <span class="n">i</span> <span class="o">=</span> <span class="o">(</span><span class="n">n</span> <span class="o">-</span> <span class="mi">1</span><span class="o">)</span> <span class="o">&amp;</span> <span class="n">hash</span><span class="o">))</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
                <span class="k">if</span> <span class="o">(</span><span class="n">casTabAt</span><span class="o">(</span><span class="n">tab</span><span class="o">,</span> <span class="n">i</span><span class="o">,</span> <span class="kc">null</span><span class="o">,</span> <span class="k">new</span> <span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;(</span><span class="n">hash</span><span class="o">,</span> <span class="n">key</span><span class="o">,</span> <span class="n">value</span><span class="o">)))</span>
                    <span class="k">break</span><span class="o">;</span>                   <span class="c1">// no lock when adding to empty bin</span>
            <span class="o">}</span>
                
            <span class="c1">// 这个桶正在迁移，执行helpTransfer帮助其迁移</span>
            <span class="k">else</span> <span class="nf">if</span> <span class="o">((</span><span class="n">fh</span> <span class="o">=</span> <span class="n">f</span><span class="o">.</span><span class="na">hash</span><span class="o">)</span> <span class="o">==</span> <span class="no">MOVED</span><span class="o">)</span>
                <span class="n">tab</span> <span class="o">=</span> <span class="n">helpTransfer</span><span class="o">(</span><span class="n">tab</span><span class="o">,</span> <span class="n">f</span><span class="o">);</span>
                
            <span class="cm">/*
                onlyIfAbsent是boolean类型。true表示仅在键不存在时put，否则不put
                fh == hash表示是否出现哈希碰撞
                
                (fk = f.key) == key || (fk != null &amp;&amp; key.equals(fk)) 
                检查当前节点的键（fk）是否与插入元素的键相同。如果键相同，则表示当前桶中已经存在这个键。

                也就是说，如果
                只在不存在key重复时才put
                并且 出现了哈希碰撞 
                并且 哈希桶的头节点的key和待插入元素的key相同
                并且 头节点的value不是null
                那么就返回头节点，不插入
                
            */</span>
            <span class="k">else</span> <span class="nf">if</span> <span class="o">(</span><span class="n">onlyIfAbsent</span> <span class="c1">// check first node without acquiring lock</span>
                     <span class="o">&amp;&amp;</span> <span class="n">fh</span> <span class="o">==</span> <span class="n">hash</span>
                     <span class="o">&amp;&amp;</span> <span class="o">((</span><span class="n">fk</span> <span class="o">=</span> <span class="n">f</span><span class="o">.</span><span class="na">key</span><span class="o">)</span> <span class="o">==</span> <span class="n">key</span> <span class="o">||</span> <span class="o">(</span><span class="n">fk</span> <span class="o">!=</span> <span class="kc">null</span> <span class="o">&amp;&amp;</span> <span class="n">key</span><span class="o">.</span><span class="na">equals</span><span class="o">(</span><span class="n">fk</span><span class="o">)))</span>
                     <span class="o">&amp;&amp;</span> <span class="o">(</span><span class="n">fv</span> <span class="o">=</span> <span class="n">f</span><span class="o">.</span><span class="na">val</span><span class="o">)</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span>
                <span class="k">return</span> <span class="n">fv</span><span class="o">;</span>
            <span class="cm">/*
                解决table[i]为null时头部数据丢失的问题
                铁了心要插入，就执行下面的代码
                先锁住哈希桶头节点
                顺着头节点往下遍历
                如果出现了key相同的节点并且onlyIfAbsent = false（已经存在key也插入，覆盖掉）
                遍历完了还没有出现key相同的节点，就新建一个节点加到链表尾部，也就是尾插法

                防止并发问题的关键代码是哪一行呢？
                synchronized (f) {
                    if (tabAt(tab, i) == f)  是加锁后的这一行
                还是以上文的AB两个线程举例
                现在tabAt(tab, i) = null， f = tabAt(tab, i) = null
                A和B都执行到了synchronized这里，竞争锁
                B竞争成功，此时语句if (tabAt(tab, i) == f) 成立，开始插入
                tabAt(tab, i) = tab[i] = new node(hash, 2,"two", null);
                然后B释放锁，A得到锁
                A执行语句if (tabAt(tab, i) == f)
                发现结果为false。tabAt(tab, i)不是null，是node(hash, 2,"two", null)
                于是A不再插入
            */</span>
            <span class="k">else</span> <span class="o">{</span>
                <span class="no">V</span> <span class="n">oldVal</span> <span class="o">=</span> <span class="kc">null</span><span class="o">;</span>
                <span class="kd">synchronized</span> <span class="o">(</span><span class="n">f</span><span class="o">)</span> <span class="o">{</span>
                    <span class="k">if</span> <span class="o">(</span><span class="n">tabAt</span><span class="o">(</span><span class="n">tab</span><span class="o">,</span> <span class="n">i</span><span class="o">)</span> <span class="o">==</span> <span class="n">f</span><span class="o">)</span> <span class="o">{</span>
                        <span class="k">if</span> <span class="o">(</span><span class="n">fh</span> <span class="o">&gt;=</span> <span class="mi">0</span><span class="o">)</span> <span class="o">{</span>
                            <span class="n">binCount</span> <span class="o">=</span> <span class="mi">1</span><span class="o">;</span>
                            <span class="k">for</span> <span class="o">(</span><span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">e</span> <span class="o">=</span> <span class="n">f</span><span class="o">;;</span> <span class="o">++</span><span class="n">binCount</span><span class="o">)</span> <span class="o">{</span>
                                <span class="no">K</span> <span class="n">ek</span><span class="o">;</span>
                                <span class="k">if</span> <span class="o">(</span><span class="n">e</span><span class="o">.</span><span class="na">hash</span> <span class="o">==</span> <span class="n">hash</span> <span class="o">&amp;&amp;</span>
                                    <span class="o">((</span><span class="n">ek</span> <span class="o">=</span> <span class="n">e</span><span class="o">.</span><span class="na">key</span><span class="o">)</span> <span class="o">==</span> <span class="n">key</span> <span class="o">||</span>
                                     <span class="o">(</span><span class="n">ek</span> <span class="o">!=</span> <span class="kc">null</span> <span class="o">&amp;&amp;</span> <span class="n">key</span><span class="o">.</span><span class="na">equals</span><span class="o">(</span><span class="n">ek</span><span class="o">))))</span> <span class="o">{</span>
                                    <span class="n">oldVal</span> <span class="o">=</span> <span class="n">e</span><span class="o">.</span><span class="na">val</span><span class="o">;</span>
                                    <span class="k">if</span> <span class="o">(!</span><span class="n">onlyIfAbsent</span><span class="o">)</span>
                                        <span class="n">e</span><span class="o">.</span><span class="na">val</span> <span class="o">=</span> <span class="n">value</span><span class="o">;</span>
                                    <span class="k">break</span><span class="o">;</span>
                                <span class="o">}</span>
                                <span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">pred</span> <span class="o">=</span> <span class="n">e</span><span class="o">;</span>
                                <span class="cm">/*
                                    table[i]不为null时
                                    下面这行不可能出现并发问题
                                    由于锁的原因
                                    后执行的A线程执行到此处时一定已经遍历过了B线程put的节点
                                    不会再出现key不同也覆盖的情况
                                */</span>
                                <span class="k">if</span> <span class="o">((</span><span class="n">e</span> <span class="o">=</span> <span class="n">e</span><span class="o">.</span><span class="na">next</span><span class="o">)</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
                                    <span class="n">pred</span><span class="o">.</span><span class="na">next</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;(</span><span class="n">hash</span><span class="o">,</span> <span class="n">key</span><span class="o">,</span> <span class="n">value</span><span class="o">);</span>
                                    <span class="k">break</span><span class="o">;</span>
                                <span class="o">}</span>
                            <span class="o">}</span>
                        <span class="o">}</span>
                        <span class="k">else</span> <span class="nf">if</span> <span class="o">(</span><span class="n">f</span> <span class="k">instanceof</span> <span class="nc">TreeBin</span><span class="o">)</span> <span class="o">{</span>
                            <span class="nc">Node</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;</span> <span class="n">p</span><span class="o">;</span>
                            <span class="n">binCount</span> <span class="o">=</span> <span class="mi">2</span><span class="o">;</span>
                            <span class="k">if</span> <span class="o">((</span><span class="n">p</span> <span class="o">=</span> <span class="o">((</span><span class="nc">TreeBin</span><span class="o">&lt;</span><span class="no">K</span><span class="o">,</span><span class="no">V</span><span class="o">&gt;)</span><span class="n">f</span><span class="o">).</span><span class="na">putTreeVal</span><span class="o">(</span><span class="n">hash</span><span class="o">,</span> <span class="n">key</span><span class="o">,</span>
                                                           <span class="n">value</span><span class="o">))</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
                                <span class="n">oldVal</span> <span class="o">=</span> <span class="n">p</span><span class="o">.</span><span class="na">val</span><span class="o">;</span>
                                <span class="k">if</span> <span class="o">(!</span><span class="n">onlyIfAbsent</span><span class="o">)</span>
                                    <span class="n">p</span><span class="o">.</span><span class="na">val</span> <span class="o">=</span> <span class="n">value</span><span class="o">;</span>
                            <span class="o">}</span>
                        <span class="o">}</span>
                        <span class="k">else</span> <span class="nf">if</span> <span class="o">(</span><span class="n">f</span> <span class="k">instanceof</span> <span class="nc">ReservationNode</span><span class="o">)</span>
                            <span class="k">throw</span> <span class="k">new</span> <span class="nf">IllegalStateException</span><span class="o">(</span><span class="s">"Recursive update"</span><span class="o">);</span>
                    <span class="o">}</span>
                <span class="o">}</span>
                <span class="k">if</span> <span class="o">(</span><span class="n">binCount</span> <span class="o">!=</span> <span class="mi">0</span><span class="o">)</span> <span class="o">{</span>
                    <span class="k">if</span> <span class="o">(</span><span class="n">binCount</span> <span class="o">&gt;=</span> <span class="no">TREEIFY_THRESHOLD</span><span class="o">)</span>
                        <span class="n">treeifyBin</span><span class="o">(</span><span class="n">tab</span><span class="o">,</span> <span class="n">i</span><span class="o">);</span>
                    <span class="k">if</span> <span class="o">(</span><span class="n">oldVal</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span>
                        <span class="k">return</span> <span class="n">oldVal</span><span class="o">;</span>
                    <span class="k">break</span><span class="o">;</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>
        <span class="n">addCount</span><span class="o">(</span><span class="mi">1L</span><span class="o">,</span> <span class="n">binCount</span><span class="o">);</span>
        <span class="k">return</span> <span class="kc">null</span><span class="o">;</span>
    <span class="o">}</span>
</code></pre></div></div>

<blockquote>
  <p>顺便吐槽一下源码的代码风格，在判断语句里给变量赋值，给我整懵了。<br />
直接看最后一个else的时候发现变量怎么定义了没赋值？仔细一看原来是在判断语句里赋值。<br />
太恶心了，就不能在定义时就赋值吗？</p>
</blockquote>]]></content><author><name>xyhao</name></author><category term="HashMap" /><category term="HashMap" /><summary type="html"><![CDATA[更多博客请见 我的语雀知识库]]></summary></entry><entry><title type="html">Kafka事务化发送消息</title><link href="/jekyll-theme-yat/kafka/2024/08/18/Kafka%E4%BA%8B%E5%8A%A1%E5%8C%96%E5%8F%91%E9%80%81%E6%B6%88%E6%81%AF.html" rel="alternate" type="text/html" title="Kafka事务化发送消息" /><published>2024-08-18T00:00:00+00:00</published><updated>2024-08-18T00:00:00+00:00</updated><id>/jekyll-theme-yat/kafka/2024/08/18/Kafka%E4%BA%8B%E5%8A%A1%E5%8C%96%E5%8F%91%E9%80%81%E6%B6%88%E6%81%AF</id><content type="html" xml:base="/jekyll-theme-yat/kafka/2024/08/18/Kafka%E4%BA%8B%E5%8A%A1%E5%8C%96%E5%8F%91%E9%80%81%E6%B6%88%E6%81%AF.html"><![CDATA[<blockquote>
  <p>更多博客请见 <a href="https://www.yuque.com/u41117719/xd1qgc">我的语雀知识库</a><br />
Kafka的事务由Broker的事务协调器负责，也有ACID特性。但一致性由于CAP限制只保证最终一致性且隔离级别只有读未提交和读已提交</p>
</blockquote>

<h2 id="示例代码">示例代码</h2>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nc">KafkaProducer</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">String</span><span class="o">&gt;</span> <span class="n">producer</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">KafkaProducer</span><span class="o">&lt;&gt;(</span><span class="n">props</span><span class="o">);</span>
        <span class="c1">// 3 初始化事务</span>
        <span class="n">producer</span><span class="o">.</span><span class="na">initTransactions</span><span class="o">();</span>
        <span class="c1">// 4 标识事务开始</span>
        <span class="n">producer</span><span class="o">.</span><span class="na">beginTransaction</span><span class="o">();</span>
        <span class="k">try</span> <span class="o">{</span>
            <span class="c1">// 5 构建待发送的消息</span>
            <span class="nc">ProducerRecord</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">String</span><span class="o">&gt;</span> <span class="n">record</span> <span class="o">=</span> <span class="kc">null</span><span class="o">;</span>
            <span class="k">for</span> <span class="o">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="o">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="mi">20</span><span class="o">;</span> <span class="n">i</span> <span class="o">++)</span> <span class="o">{</span>
                <span class="n">record</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ProducerRecord</span><span class="o">&lt;&gt;(</span><span class="n">topicName</span><span class="o">,</span> <span class="n">msg</span><span class="o">);</span>
                <span class="c1">// 6 发送消息</span>
                <span class="n">producer</span><span class="o">.</span><span class="na">send</span><span class="o">(</span><span class="n">record</span><span class="o">);</span>
                <span class="c1">// 省略一些其他逻辑</span>
            <span class="o">}</span>
            <span class="c1">// 7 commit事务</span>
            <span class="n">producer</span><span class="o">.</span><span class="na">commitTransaction</span><span class="o">();</span>
        <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">ProducerFencedException</span> <span class="n">e1</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">e1</span><span class="o">.</span><span class="na">printStackTrace</span><span class="o">();</span>
            <span class="n">producer</span><span class="o">.</span><span class="na">close</span><span class="o">();</span>
        <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">KafkaException</span> <span class="n">e2</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">e2</span><span class="o">.</span><span class="na">printStackTrace</span><span class="o">();</span>
            <span class="n">producer</span><span class="o">.</span><span class="na">abortTransaction</span><span class="o">();</span>
        <span class="o">}</span>

        <span class="c1">// 8 关闭生产者实例</span>
        <span class="n">producer</span><span class="o">.</span><span class="na">close</span><span class="o">();</span>
    <span class="o">}</span>
</code></pre></div></div>

<h2 id="事务执行原理">事务执行原理</h2>
<h3 id="初始化事务">初始化事务</h3>

<ol>
  <li>首先，需要在生产者客户端配置 producerProps.put(“transactional.id”, “ShiWuID”);，设置 事务id，可以让程序重启之后被识别为同一个事务生产者。</li>
  <li>当执行<code class="language-plaintext highlighter-rouge">producer.initTransactions()</code>后，生产者会与某个Broker端的事务协调器产生联系。具体怎么确定是哪个Broker呢？
    <ol>
      <li>在 Kafka 中有个特殊的事务 topic，名称为 __transaction_state ，负责持久化事务消息。该 topic 默认有 50 个Partition，每个Parition负责一部分事务。</li>
      <li>事务划分规则是当生产者向 bootstrap.servers 中任何一个地址发送请求时，该 Broker 会先计算 事务id 的哈希值，然后将该哈希值 % 50 得到一个计算结果，该结果就是 __transaction_state 的某个Partition</li>
      <li>由该分区的 Leader 副本所在的 Broker 节点就是该生产者所属的事务协调器。随后就会向生产者反馈事务协调器的地址</li>
    </ol>
  </li>
</ol>

<h3 id="发送消息">发送消息</h3>
<p>生产者在每次发送消息的请求中都会包含 PID、Epoch 和 事务id 以及其他字段。而生产者在每次调用 producer.send() 方法时，需要循环执行以下两个步骤：</p>

<ol>
  <li>AddPartitionsToTxnRequest：将本次发送请求涉及到的 TopicPartition 添加到事务协调器对应的 __transaction_state 的某个分区中，这样当事务提交之后，事务协调器会将 __transaction_state 分区中 TopicPartition 的状态标记为已提交(Commit)或放弃(Abort)。</li>
  <li>ProduceRequest：对于每个 PID、Epoch 和 事务id 组合，生产者都会维护一个序列号，该序列号从 0 开始，每条消息的序列号都会单调递增。Broker也会维护每个 PID、Epoch 和 Transactional ID 组合的序列号，如果当前序列号不比前一个序列号大 1，就会拒绝请求。</li>
</ol>

<h3 id="提交事务">提交事务</h3>
<p>根据事务的原子性，要么全部成功，要么全部回滚。生产者就会向事务协调器调用 commitTransaction() 或 abortTransaction()，从而完成事务。</p>

<h3 id="消费者消费">消费者消费</h3>
<p>以下两次调用是消费者端的事务一致性功能</p>

<ol>
  <li>AddOffsetsToTxnRequest：消费者会通过 AddOffsetsToTxnRequest 将 Consumer Group ID 信息传递给事务协调器，事务协调器从中提炼出消费者订阅的 TopicPartition，并将其记录在事务日志中。</li>
  <li>TxnOffsetCommitRequest：当消费者成功消费完数据之后，会在内部通过调用 TxnOffsetCommitRequest，将该 TopicPartition 和 消息 offset 的写到 __consumer_offsets 主题中，表示数据已经成功消费。注意，在生产者发送消息的事务完成之前，消费者是无法看到这些消息的 offset 的。即当消费者将隔离级别设置为isolation.level = read_committed时，消费者看不见未完成状态的消息，只能看见事务已提交状态的消息。</li>
</ol>

<h2 id="事务协调器解决数据不一致问题">事务协调器解决数据不一致问题</h2>
<p>先举个例子：生产者向topic1发送两条消息。下游消费者等待事务提交后消费这两条消息
整体执行流程如下：</p>

<ol>
  <li>生产者开启事务，锁定事务协调器</li>
  <li>向topic1的Partition1发送两条消息，同时向事务协调器发送topic1，事务协调器记录topic1的Partition1参与了该事务。事务协调器还会向__transaction_state这个topic里写入一条控制消息，记录这条消息所属的事务id。</li>
  <li>事务协调器维护topic1的Partition1的LSO（log stable offset）。一个Partition中存了多个事务消息，也存储了很多非事务的普通消息，而LSO为第一个正在进行中的事务消息的offset。<img src="/assets/articleSource/2024-08-18-Kafka/img_1.png" alt="image.png" /></li>
</ol>

<p>如上图:<br />
a： 已经无效的事务
b： 已经提交的事务
c： 正在进行中的事务（不确定最终是取消还是提交）
d： 普通消息，非事务消息
在LSO之后的位置(上图LSO右边)的消息是不可以被隔离级别为读已提交的消费者消费的</p>

<ol>
  <li>生产者提交事务，向事务协调器发送信号，事务协调器修改事务状态的同时，通知本次事务涉及到的所有Partition更新LSO。</li>
  <li>生产者主动回滚事务或触发超时，事务协调器主动回滚事务。事务协调器通知各个partiton更新LSO并将本次事务产生的消息作废。</li>
</ol>

<p><img src="/assets/articleSource/2024-08-18-Kafka/img.png" alt="image.png" /></p>

<p>Kafka事务的整体流程是分布式事务里标准的两阶段提交<br />
<a href="https://www.yuque.com/u41117719/xd1qgc/rs1c36guisudkgm8">分布式事务</a></p>

<h2 id="跨会话和跨-partition-的幂等性">跨会话和跨 Partition 的幂等性</h2>
<ul>
  <li>跨会话幂等性：</li>
</ul>

<p>Kafka 的事务机制保证了即使在生产者会话之间切换，基于相同事务 ID 的操作仍然保持幂等性。如果生产者崩溃并重启，只要事务 ID 是相同的，Kafka 会确保不会重复处理同一事务的消息。</p>
<ul>
  <li>跨 Partition 幂等性：</li>
</ul>

<p>Kafka 的事务机制允许生产者在多个 Topic 和分区中发送消息，并通过事务 ID 维护幂等性。即使消息分布在多个分区中，Kafka 会确保所有相关的消息在事务提交前不会被重复写入或丢失。</p>

<h2 id="一些细节">一些细节</h2>

<ol>
  <li><strong>问题1:</strong> 上图3.1向<code class="language-plaintext highlighter-rouge">__transaction_state</code>主题写完事务状态后，便给Producer回应说事务提交成功，假如说3.2执行过程中被hang住了，在Producer看来，既然事务已经提交成功，为什么还是读不到对应消息呢？
    <ol>
      <li>的确是这样，这里成功指的是Coordinator收到了消息，并且成功修改了事务状态。因此<strong>返回成功的语义指的是一阶段提交成功</strong>，因为后续向各个Partition发送写marker的会无限重试，直至成功</li>
    </ol>
  </li>
  <li><strong>问题2:</strong> 上图3.2中向多个Broker发送marker消息，如果Broker1、Broker2均写入成功了，但是Broker3因为网络抖动，Coordinator还在重试，那么此时Broker1、Broker2上的消息对Consumer来说已经可见了，但是Broker3上的消息还是看不到，这不就不符合事务语义了吗？
    <ol>
      <li>事实确实如此，所以kafka的事务不能保证强一致性，并不是说kafka做的不够完美，而是这种分布式事务统一存在类似的问题，CAP铁律限制，这里只能做到最终一致性了。不过对于常规的场景这里已经够用了，Coordinator会不遗余力的重试，直至成功</li>
    </ol>
  </li>
  <li><strong>问题3</strong>：LSO之前有3条消息，2个a（事务取消），1个b（事务提交），consumer读到这3条消息后怎么处理呢？无非就是以下两种处理逻辑。
    <ol>
      <li>暂存在consumer端，直至读取到事务最终状态，再来判断是吐给业务端（事务成功），还是消息扔掉（事务取消）
        <ol>
          <li>这样设计是没有问题的，可以保证消息的准确性，但是如果某个事务提交的数据量巨大（事务最长超时时间可达15分钟），这样势必造成consumer端内存吃紧，甚至OOM</li>
        </ol>
      </li>
      <li>实时判断当前消息是该成功消费还是被扔掉
        <ol>
          <li>能够实时判断肯定是非常理想的结果，可是如何实时判断呢？难道每次消费时都要再向broker发送请求获取消息的状态吗？</li>
        </ol>
      </li>
      <li>Kafka具体采用哪种策略？<br />实际上Partition依托于__transaction_state主题里的控制消息，是可以得知消息状态的。Partiton 会根据 <code class="language-plaintext highlighter-rouge">LSO</code> 和事务日志的记录，只返回已提交的消息。消费者不需要查询或额外判断消息的状态 。</li>
    </ol>
  </li>
</ol>

<blockquote>
  <p><a href="https://www.cnblogs.com/xijiu/p/16917741.html">Kafka事务「原理剖析」 - 昔久 - 博客园</a></p>
</blockquote>]]></content><author><name>xyhao</name></author><category term="Kafka" /><category term="Java" /><category term="Kafka" /><summary type="html"><![CDATA[更多博客请见 我的语雀知识库 Kafka的事务由Broker的事务协调器负责，也有ACID特性。但一致性由于CAP限制只保证最终一致性且隔离级别只有读未提交和读已提交]]></summary></entry><entry><title type="html">Kafka发送消息的三种模式</title><link href="/jekyll-theme-yat/kafka/2024/08/18/Kafka%E5%8F%91%E9%80%81%E6%B6%88%E6%81%AF%E7%9A%84%E4%B8%89%E7%A7%8D%E6%A8%A1%E5%BC%8F.html" rel="alternate" type="text/html" title="Kafka发送消息的三种模式" /><published>2024-08-18T00:00:00+00:00</published><updated>2024-08-18T00:00:00+00:00</updated><id>/jekyll-theme-yat/kafka/2024/08/18/Kafka%E5%8F%91%E9%80%81%E6%B6%88%E6%81%AF%E7%9A%84%E4%B8%89%E7%A7%8D%E6%A8%A1%E5%BC%8F</id><content type="html" xml:base="/jekyll-theme-yat/kafka/2024/08/18/Kafka%E5%8F%91%E9%80%81%E6%B6%88%E6%81%AF%E7%9A%84%E4%B8%89%E7%A7%8D%E6%A8%A1%E5%BC%8F.html"><![CDATA[<blockquote>
  <p>更多博客请见 <a href="https://www.yuque.com/u41117719/xd1qgc">我的语雀知识库</a><br />
省流：推荐使用带回调方法的异步发送</p>
</blockquote>

<h3 id="发后即忘">发后即忘</h3>
<p>这种模式只管往broker里发数据，但是broker收没收到它不管。
通常情况下，这种发后即忘(fire-and-forget)的方式并不会出现问题，但也有意外情况：</p>

<ol>
  <li>遇到可重试异常，如果发送消息到broker时抛出异常，且是可重试异常，就会去重试执行方法。org.apache.kafka.clients.producer.internals.Sender类中有如下方法：
    <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">private</span> <span class="kt">boolean</span> <span class="nf">canRetry</span><span class="o">(</span><span class="nc">ProducerBatch</span> <span class="n">batch</span><span class="o">,</span> <span class="nc">ProduceResponse</span><span class="o">.</span><span class="na">PartitionResponse</span> <span class="n">response</span><span class="o">)</span> <span class="o">{</span>
 <span class="k">return</span> <span class="n">batch</span><span class="o">.</span><span class="na">attempts</span><span class="o">()</span> <span class="o">&lt;</span> <span class="k">this</span><span class="o">.</span><span class="na">retries</span> <span class="o">&amp;&amp;</span>
         <span class="o">((</span><span class="n">response</span><span class="o">.</span><span class="na">error</span><span class="o">.</span><span class="na">exception</span><span class="o">()</span> <span class="k">instanceof</span> <span class="nc">RetriableException</span><span class="o">)</span> <span class="o">||</span>
                 <span class="o">(</span><span class="n">transactionManager</span> <span class="o">!=</span> <span class="kc">null</span> <span class="o">&amp;&amp;</span> <span class="n">transactionManager</span><span class="o">.</span><span class="na">canRetry</span><span class="o">(</span><span class="n">response</span><span class="o">,</span> <span class="n">batch</span><span class="o">)));</span>
<span class="o">}</span>
</code></pre></div>    </div>
    <p>允许重试需要满足两个条件：</p>
  </li>
  <li>重试次数少于参数retries指定的值；</li>
  <li>异常是RetriableException类型或者TransactionManager允许重试；
    <h3 id="同步发送">同步发送</h3>
    <p>严格上来说，Kafka并没有同步发送机制。因为发送消息的send方法本身是异步的。
但是Kafka也有提供一些机制让我们做到同步发送。
具体怎么做到同步发送呢？</p>
  </li>
</ol>

<ul>
  <li>send方法是有返回值的，返回值是Future类型，这是一个接口，可以提供异步的执行结果。我们可以使用Future的get方法去等待异步结束。这样就把异步改成了同步。</li>
</ul>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">try</span> <span class="o">{</span>
        <span class="c1">// RecordMetadata包含了成功发送到 Kafka Broker 的消息的元数据信息</span>
        <span class="nc">Future</span><span class="o">&lt;</span><span class="nc">RecordMetadata</span><span class="o">&gt;</span> <span class="n">future</span> <span class="o">=</span> <span class="n">producer</span><span class="o">.</span><span class="na">send</span><span class="o">(</span><span class="n">record</span><span class="o">);</span>
        <span class="nc">RecordMetadata</span> <span class="n">metadata</span> <span class="o">=</span> <span class="n">future</span><span class="o">.</span><span class="na">get</span><span class="o">();</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">printf</span><span class="o">(</span><span class="s">"Send Record(key=%s value=%s) "</span> <span class="o">+</span>
                        <span class="s">"meta(Partition=%d, offset=%d) \n"</span><span class="o">,</span>
                <span class="n">record</span><span class="o">.</span><span class="na">key</span><span class="o">(),</span> <span class="n">record</span><span class="o">.</span><span class="na">value</span><span class="o">(),</span>
                <span class="n">metadata</span><span class="o">.</span><span class="na">partition</span><span class="o">(),</span> <span class="n">metadata</span><span class="o">.</span><span class="na">offset</span><span class="o">());</span>
    <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">InterruptedException</span> <span class="o">|</span> <span class="nc">ExecutionException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">e</span><span class="o">.</span><span class="na">printStackTrace</span><span class="o">();</span>
    <span class="o">}</span>

</code></pre></div></div>

<ul>
  <li>如果遇到可重试异常，在超出最大重试次数之前如果能解决，我们的代码就不会捕获到异常，否则就会爆出异常需要捕获</li>
  <li>如果是不可重试异常，是肯定要做异常捕获的。做好异常捕获记录详细日志信息，日后排查问题也方便</li>
</ul>

<p>同步发送的可靠性比较高，如果发生异常，可以捕获并处理异常；并不会像 “发后即忘” 的方式直接造成消息丢失。但是，<strong>同步发送的方式性能会比较差，需要阻塞等待一条消息发送成功之后才会发送下一条消息。</strong>kafka的批消息发送也就成摆设了。</p>

<h3 id="异步发送">异步发送</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">producer</span><span class="o">.</span><span class="na">send</span><span class="o">(</span><span class="n">record</span><span class="o">,</span> <span class="k">new</span> <span class="nc">Callback</span><span class="o">()</span> <span class="o">{</span>
        <span class="nd">@Override</span>
        <span class="kd">public</span> <span class="kt">void</span> <span class="nf">onCompletion</span><span class="o">(</span><span class="nc">RecordMetadata</span> <span class="n">metadata</span><span class="o">,</span> <span class="nc">Exception</span> <span class="n">exception</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">if</span> <span class="o">(</span><span class="n">metadata</span> <span class="o">!=</span> <span class="kc">null</span> <span class="o">)</span> <span class="o">{</span>
                <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">printf</span><span class="o">(</span><span class="s">"Send Record(key=%s value=%s) "</span> <span class="o">+</span>
                                <span class="s">"meta(Partition=%d, offset=%d) \n"</span><span class="o">,</span>
                        <span class="n">record</span><span class="o">.</span><span class="na">key</span><span class="o">(),</span> <span class="n">record</span><span class="o">.</span><span class="na">value</span><span class="o">(),</span>
                        <span class="n">metadata</span><span class="o">.</span><span class="na">partition</span><span class="o">(),</span> <span class="n">metadata</span><span class="o">.</span><span class="na">offset</span><span class="o">());</span>
            <span class="o">}</span>
            <span class="k">if</span> <span class="o">(</span><span class="n">exception</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
                <span class="n">exception</span><span class="o">.</span><span class="na">printStackTrace</span><span class="o">();</span>
            <span class="o">}</span>
        <span class="o">}</span>
    <span class="o">});</span>
</code></pre></div></div>

<p>异步发送消息，比前两种方法多了一个回调函数的参数
不需要 try/catch，因为异常会在回调函数中处理。
回调方法中的 onCompletion() 方法中的两个参数是互斥的。消息发送成功时，metadata 不为 null 而 exception 为 null，反之则反之。
回调方法的真正优势在于发送数据失败时，可以在数据发送失败时采取一些措施，如抛出异常、记录错误或将失败的数据写入到其他地方进行分析。很显然，<strong>使用带回调函数的异步发送方式既高效、又优雅</strong>，通常在生产环境中都建议使用这种方式发送消息。</p>

<p>:::warning
注意：回调函数是在生产者的主线程中执行的。必须要确保回调方法的执行速度足够快，<strong>绝对不能在回调方法中产生阻塞</strong>，否则会减慢生产者的运行速度。
:::</p>]]></content><author><name>xyhao</name></author><category term="Kafka" /><category term="Java" /><category term="Kafka" /><summary type="html"><![CDATA[更多博客请见 我的语雀知识库 省流：推荐使用带回调方法的异步发送]]></summary></entry><entry><title type="html">Kafka如何保证消息不重复</title><link href="/jekyll-theme-yat/kafka/2024/08/18/Kafka%E5%A6%82%E4%BD%95%E4%BF%9D%E8%AF%81%E6%B6%88%E6%81%AF%E4%B8%8D%E9%87%8D%E5%A4%8D.html" rel="alternate" type="text/html" title="Kafka如何保证消息不重复" /><published>2024-08-18T00:00:00+00:00</published><updated>2024-08-18T00:00:00+00:00</updated><id>/jekyll-theme-yat/kafka/2024/08/18/Kafka%E5%A6%82%E4%BD%95%E4%BF%9D%E8%AF%81%E6%B6%88%E6%81%AF%E4%B8%8D%E9%87%8D%E5%A4%8D</id><content type="html" xml:base="/jekyll-theme-yat/kafka/2024/08/18/Kafka%E5%A6%82%E4%BD%95%E4%BF%9D%E8%AF%81%E6%B6%88%E6%81%AF%E4%B8%8D%E9%87%8D%E5%A4%8D.html"><![CDATA[<blockquote>
  <p>更多博客请见 <a href="https://www.yuque.com/u41117719/xd1qgc">我的语雀知识库</a><br />
Kafka支持幂等操作，保证了消息不重复，不丢失。从Kafka3.0版本开始默认开启幂等。<br />
开启幂等需要配置消息发送的重试次数</p>
  <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 开启生产者幂等</span>
<span class="n">props</span><span class="o">.</span><span class="na">setProperty</span><span class="o">(</span><span class="nc">ProducerConfig</span><span class="o">.</span><span class="na">ENABLE_IDEMPOTENCE_CONFIG</span><span class="o">,</span> <span class="s">"true"</span><span class="o">);</span> 
<span class="n">props</span><span class="o">.</span><span class="na">setProperty</span><span class="o">(</span><span class="nc">ProducerConfig</span><span class="o">.</span><span class="na">ACKS_CONFIG</span><span class="o">,</span> <span class="s">"all"</span><span class="o">);</span> 
<span class="c1">// 配置重试次数</span>
<span class="n">props</span><span class="o">.</span><span class="na">setProperty</span><span class="o">(</span><span class="nc">ProducerConfig</span><span class="o">.</span><span class="na">RETRIES_CONFIG</span><span class="o">,</span> <span class="nc">Integer</span><span class="o">.</span><span class="na">toString</span><span class="o">(</span><span class="nc">Integer</span><span class="o">.</span><span class="na">MAX_VALUE</span><span class="o">));</span> 
<span class="c1">// 如果 Kafka 版本 &gt;= 2.0，可配置为5；否则配置成 1</span>
<span class="n">props</span><span class="o">.</span><span class="na">setProperty</span><span class="o">(</span><span class="nc">ProducerConfig</span><span class="o">.</span><span class="na">MAX_IN_FLIGHT_REQUESTS_PER_CONNECTION</span><span class="o">,</span> <span class="s">"5"</span><span class="o">);</span> 
</code></pre></div>  </div>
</blockquote>

<h2 id="为什么要设置发送消息的重试次数">为什么要设置发送消息的重试次数</h2>
<p>当生产者向 Broker 端发送消息失败时，Broker 端会向生产者端返回错误码。因此我们需要重新发送该消息。这是尽量避免消息丢失。但是会有这样一种情况：
第一次发送消息时，虽然 Broker 已经收到消息了，但由于网络抖动等原因导致 Broker 向生产者反馈的 ack 失败。因此，生产者会重新发送这批消息，最终导致消息重复。
但还是要开启重试机制。重复的问题留给幂等性解决。</p>

<h2 id="幂等如何做到">幂等，如何做到？</h2>
<h3 id="做到不重复">做到不重复</h3>
<p>要做到幂等性，要解决下面的问题：</p>

<ol>
  <li>Broker 端能够鉴别一条数据到底是不是重复的数据？最常用的手段是通过 唯一键/唯一id 来判断，因此需要在 Broker 端缓存已经处理的唯一键记录，这样才能判断出新发送过来的数据是否重复。</li>
  <li>唯一键应该选择什么粒度？对于分布式系统来说，肯定不建议使用全局唯一键。Kafka 在这方面的考虑是在分区的维度上面设计唯一键，让每个分区的 Leader 判断数据是否重复。</li>
  <li>将唯一键设计到分区粒度是否有问题？有可能有多个生产者同时向一个 Partition 中写入数据，各个生产者都是独立的个体，他们之间的通信比较困难。因此，选择设计的唯一键是 Producer + TopicPartition 维度。</li>
  <li>如何保证消息的顺序性？生产者在发送信息时会同时发送一个序列号，序列号以 TopicPartition 为基础，从 0 开始，并随着每条信息的产生而递增。在 Broker 端通过序列号是否连续来判断消息是否是顺序发送。</li>
</ol>

<p>出于对以上内存的考虑，Kafka 要做到幂等性，就要做到以下几个方面：</p>
<h4 id="broker角度">Broker角度</h4>

<ol>
  <li><strong>PID</strong>（Producer ID）：给每个生产者都指定的生产者 ID（PID）。</li>
  <li><strong>Sequence Number</strong>：生产者在发送信息时会发送一个序列号，序列号以 TopicPartition 为基础，从 0 开始，并随着每条信息的产生而递增。Broker 会存储每个生产者为给定的 TopicPartition 发送的序列号，并据此判断该消息是否是：
    <ul>
      <li>新消息：Broker 新收到的消息的序列号刚好比给定生产者在本地存储的序列号大 1。</li>
      <li>重复消息：Broker 新收到的消息的序列号小于等于给定生产者保存的最大序列号。</li>
      <li>失序：Broker 新收到的消息序列号 - 本地最大序列号的差值大于 1，这意味着有些消息丢失了。</li>
    </ul>
  </li>
</ol>

<h4 id="producer角度">Producer角度</h4>
<p>上面是从服务器角度考虑的幂等性实现。但如果我们能在消息发出去之前就判断出这条消息是重复的岂不更好？
Kafka的消息是批发送的，批消息叫做ProducerBatch，在一个ProducerBatch中的消息都是要发往同一个Partition的。<strong>如果一条消息重复发送那么其所属的ProducerBatch一定也重复发送了。</strong>
每当有新的 ProducerBatch 被添加到 RecordAccumulator 时，<strong>就会有新的元数据 batchMetadata 被添加到batchMetadata队列中（该队列最大为5）</strong>，一个batchMetadata 对应一个ProducerBatch。而 batchMetadata 中主要包括lastSeq、lastOffset、offsetDelta 和 timestamp 等元数据。</p>

<ol>
  <li>lastSeq：序列号是每条添加到 ProducerBatch 的编号，而 lastSeq 记录的是每个 ProducerBatch 的最后一条消息的序列号。</li>
  <li>lastOffset：每个 ProducerBatch 中的最后一条消息的 offset。</li>
  <li>offsetDelta ：每个 ProducerBatch 中，最后一条消息和第一条消息的 offset 差值。lastSeq - offsetDelta 可以得到第一条消息的 seq，lastOffset - offsetDelta 可以得到第一条消息的 offset。</li>
  <li>timestamp：每个 ProducerBatch 最后一条消息的添加时间。</li>
</ol>

<p>在Producer内部有专门的方法校验ProducerBatch是否有重复消息：</p>

<ul>
  <li>遍历 batchMetadata队列中的元数据，如果新发送的 ProducerBatch 的 firstSeq 和 lastSeq 都和 batchMetadata队列中缓存的某个 batchMetadata 相同，说明是重复批次。</li>
</ul>

<p>简单说就是：
<strong>Producer维护了发往某个Partition的最近五个批次的元数据。里面保存了每一个批次的最新和最旧序列号。如果现在准备发送的批消息的最新最旧序列号和最近五个批消息的最新最近系列号重合了，说明是重复消息。不予发送。</strong>
<strong>为什么是最近五个？因为源代码里写死了。</strong></p>

<h3 id="做到不乱序">做到不乱序</h3>
<p>存储ProducerBatch的是一个队列。假如现在情况如下：</p>

<table>
  <tbody>
    <tr>
      <td>【</td>
      <td> </td>
      <td> </td>
      <td> </td>
      <td> </td>
      <td> </td>
      <td> </td>
      <td> </td>
      <td> </td>
      <td> </td>
      <td>】                       &lt;— 1 2 3 4 5 6 7 8 9</td>
    </tr>
  </tbody>
</table>

<p>顺序发送数据包123，456，789。但是由于网络链路的原因，123先到，789次之，造成了乱序</p>

<table>
  <tbody>
    <tr>
      <td>【</td>
      <td>1</td>
      <td>2</td>
      <td>3</td>
      <td>7</td>
      <td>8</td>
      <td>9</td>
      <td> </td>
      <td> </td>
      <td> </td>
      <td>】 4 5 6   &lt;— Empty Queue</td>
    </tr>
  </tbody>
</table>

<p>如果不开启幂等性，Broker不管任何顺序都是接收不误的。一旦开启了幂等性，Broker就会拒接接收789数据包。将789退还给双端队列。</p>

<table>
  <tbody>
    <tr>
      <td>【</td>
      <td>1</td>
      <td>2</td>
      <td>3</td>
      <td> </td>
      <td> </td>
      <td> </td>
      <td> </td>
      <td> </td>
      <td> </td>
      <td>】 4 5 6         &lt;—                  7 8 9</td>
    </tr>
  </tbody>
</table>

<p>接收456，再接收789</p>

<table>
  <tbody>
    <tr>
      <td>【</td>
      <td>1</td>
      <td>2</td>
      <td>3</td>
      <td>4</td>
      <td>5</td>
      <td>6</td>
      <td> </td>
      <td> </td>
      <td> </td>
      <td>】            &lt;—                  7 8 9</td>
    </tr>
  </tbody>
</table>

<p>这样就解决了乱序问题了。</p>

<h2 id="总结">总结</h2>
<p>这样实现的幂等还是有缺陷的：</p>

<ol>
  <li>只能保证 Producer 在单个会话内消息的不丟不重不乱，如果 Producer 出现意外宕机再重启是无法保证的（由于在幂等性的场景下，无法获取 Producer 宕机之前的状态信息比如PID，PID每次都是不一样的。因此是无法做到跨会话级别的不丢不重不乱）;</li>
  <li>幂等性不能跨多个 TopicPartition，只能保证 Producer 在单个 TopicPartition 内的幂等性，当涉及多个 TopicPartition 时，这些状态信息并没有同步。</li>
</ol>

<p><strong>如果需要实现跨会话、跨多个 Topic-Partition 的幂等性，需要使用 Kafka 的事务性来实现。</strong></p>]]></content><author><name>xyhao</name></author><category term="Kafka" /><category term="Java" /><category term="Kafka" /><summary type="html"><![CDATA[更多博客请见 我的语雀知识库 Kafka支持幂等操作，保证了消息不重复，不丢失。从Kafka3.0版本开始默认开启幂等。 开启幂等需要配置消息发送的重试次数 // 开启生产者幂等 props.setProperty(ProducerConfig.ENABLE_IDEMPOTENCE_CONFIG, "true"); props.setProperty(ProducerConfig.ACKS_CONFIG, "all"); // 配置重试次数 props.setProperty(ProducerConfig.RETRIES_CONFIG, Integer.toString(Integer.MAX_VALUE)); // 如果 Kafka 版本 &gt;= 2.0，可配置为5；否则配置成 1 props.setProperty(ProducerConfig.MAX_IN_FLIGHT_REQUESTS_PER_CONNECTION, "5");]]></summary></entry><entry><title type="html">Kafka路由策略</title><link href="/jekyll-theme-yat/kafka/2024/08/18/Kafka%E8%B7%AF%E7%94%B1%E7%AD%96%E7%95%A5.html" rel="alternate" type="text/html" title="Kafka路由策略" /><published>2024-08-18T00:00:00+00:00</published><updated>2024-08-18T00:00:00+00:00</updated><id>/jekyll-theme-yat/kafka/2024/08/18/Kafka%E8%B7%AF%E7%94%B1%E7%AD%96%E7%95%A5</id><content type="html" xml:base="/jekyll-theme-yat/kafka/2024/08/18/Kafka%E8%B7%AF%E7%94%B1%E7%AD%96%E7%95%A5.html"><![CDATA[<blockquote>
  <p>更多博客请见 <a href="https://www.yuque.com/u41117719/xd1qgc">我的语雀知识库</a><br />
生产者的路由策略决定了消息应该写入哪个分区。选择合适的路由策略可以优化生产者的性能和负载均衡 。</p>
</blockquote>

<h2 id="粘性路由策略">粘性路由策略</h2>

<ul>
  <li>粘性路由策略尝试将一段时间内的消息发送到相同的分区。这个策略在生产者启动时选择一个分区，并在接下来的时间里尽量将所有消息发送到这个分区。只有在当前分区达到一定条件（如大小或数量限制）时，生产者才会切换到其他分区。</li>
  <li><strong>优点</strong>：
    <ul>
      <li><strong>减少分区切换</strong>：减少了生产者频繁切换分区的次数，从而提高了写入效率。</li>
      <li><strong>提高吞吐量</strong>：集中写入同一个分区可以提高磁盘的写入性能，特别是在高负载场景下。</li>
      <li><strong>改善负载均衡</strong>：粘性路由可以在时间范围内平衡分区负载，避免短时间内的负载不均。</li>
      <li><strong>适用场景</strong>：适合需要高吞吐量和较少分区切换的场景，如大批量消息写入和高性能的数据写入场景。</li>
    </ul>
  </li>
</ul>

<h2 id="轮询">轮询</h2>
<p>默认的分区路由策略。生产者将消息均匀地分配到 Topic 的所有分区中。每个消息会被依次发送到不同的分区，以实现负载均衡。</p>

<ul>
  <li><strong>优点</strong>：简单易用，能够均匀地分配消息负载。</li>
  <li><strong>适用场景</strong>：适合消息负载比较均匀的场景。</li>
</ul>

<h2 id="基于键的路由">基于键的路由</h2>
<p>生产者根据消息的key的哈希值来决定分区。相同的键将始终路由到同一个分区。  这个路由需要我们在发送消息时指定key。send方法有一个重载版本可以传入key。</p>

<ul>
  <li><strong>优点</strong>：能够确保同一类型的消息（相同键）始终发送到同一个分区，有利于顺序消费和数据局部性。</li>
  <li><strong>适用场景</strong>：需要确保相关数据在同一分区的场景，例如用户会话数据或交易记录。</li>
</ul>

<p><strong>如果我们执行send方法时指定了key，不管配置了什么路由都会选择基于键的路由。</strong></p>]]></content><author><name>xyhao</name></author><category term="Kafka" /><category term="Kafka" /><summary type="html"><![CDATA[更多博客请见 我的语雀知识库 生产者的路由策略决定了消息应该写入哪个分区。选择合适的路由策略可以优化生产者的性能和负载均衡 。]]></summary></entry><entry><title type="html">Arthas实战-排查死锁</title><link href="/jekyll-theme-yat/java/2024/08/18/arthas%E5%AE%9E%E6%88%98-%E6%AD%BB%E9%94%81%E6%8E%92%E6%9F%A5.html" rel="alternate" type="text/html" title="Arthas实战-排查死锁" /><published>2024-08-18T00:00:00+00:00</published><updated>2024-08-18T00:00:00+00:00</updated><id>/jekyll-theme-yat/java/2024/08/18/arthas%E5%AE%9E%E6%88%98%EF%BC%9A%E6%AD%BB%E9%94%81%E6%8E%92%E6%9F%A5</id><content type="html" xml:base="/jekyll-theme-yat/java/2024/08/18/arthas%E5%AE%9E%E6%88%98-%E6%AD%BB%E9%94%81%E6%8E%92%E6%9F%A5.html"><![CDATA[<blockquote>
  <p>更多博客请见 <a href="https://www.yuque.com/u41117719/xd1qgc">我的语雀知识库</a></p>
</blockquote>

<h2 id="一-准备测试应用">一、 准备测试应用</h2>
<ol>
  <li>新建一个测试程序，写一段线程死锁的代码：</li>
</ol>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">java.util.*</span><span class="o">;</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">Test</span> <span class="o">{</span>
    <span class="kd">static</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">resource1</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>
    <span class="kd">static</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">resource2</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">threadLock</span><span class="o">();</span>
    <span class="o">}</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">threadLock</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">Thread</span> <span class="n">thread1</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Thread</span><span class="o">(()</span> <span class="o">-&gt;</span> <span class="o">{</span>
            <span class="kd">synchronized</span> <span class="o">(</span><span class="n">resource1</span><span class="o">)</span> <span class="o">{</span>
                <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="nc">Thread</span><span class="o">.</span><span class="na">currentThread</span><span class="o">().</span><span class="na">getName</span><span class="o">()</span> <span class="o">+</span> <span class="s">" got resource1 lock."</span><span class="o">);</span>
                <span class="k">try</span> <span class="o">{</span>
                    <span class="nc">Thread</span><span class="o">.</span><span class="na">sleep</span><span class="o">(</span><span class="mi">100</span><span class="o">);</span> <span class="c1">// 模拟工作时间，让死锁更容易观察到</span>
                <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">InterruptedException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
                    <span class="n">e</span><span class="o">.</span><span class="na">printStackTrace</span><span class="o">();</span>
                <span class="o">}</span>
                <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="nc">Thread</span><span class="o">.</span><span class="na">currentThread</span><span class="o">().</span><span class="na">getName</span><span class="o">()</span> <span class="o">+</span> <span class="s">" trying to get resource2 lock."</span><span class="o">);</span>
                <span class="kd">synchronized</span> <span class="o">(</span><span class="n">resource2</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// 尝试获取resource2的锁</span>
                    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="nc">Thread</span><span class="o">.</span><span class="na">currentThread</span><span class="o">().</span><span class="na">getName</span><span class="o">()</span> <span class="o">+</span> <span class="s">" got resource2 lock."</span><span class="o">);</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">},</span> <span class="s">"Thread 1"</span><span class="o">);</span>

        <span class="nc">Thread</span> <span class="n">thread2</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Thread</span><span class="o">(()</span> <span class="o">-&gt;</span> <span class="o">{</span>
            <span class="kd">synchronized</span> <span class="o">(</span><span class="n">resource2</span><span class="o">)</span> <span class="o">{</span>
                <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="nc">Thread</span><span class="o">.</span><span class="na">currentThread</span><span class="o">().</span><span class="na">getName</span><span class="o">()</span> <span class="o">+</span> <span class="s">" got resource2 lock."</span><span class="o">);</span>
                <span class="k">try</span> <span class="o">{</span>
                    <span class="nc">Thread</span><span class="o">.</span><span class="na">sleep</span><span class="o">(</span><span class="mi">100</span><span class="o">);</span> <span class="c1">// 模拟工作时间</span>
                <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">InterruptedException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
                    <span class="n">e</span><span class="o">.</span><span class="na">printStackTrace</span><span class="o">();</span>
                <span class="o">}</span>
                <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="nc">Thread</span><span class="o">.</span><span class="na">currentThread</span><span class="o">().</span><span class="na">getName</span><span class="o">()</span> <span class="o">+</span> <span class="s">" trying to get resource1 lock."</span><span class="o">);</span>
                <span class="kd">synchronized</span> <span class="o">(</span><span class="n">resource1</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// 尝试获取resource1的锁</span>
                    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="nc">Thread</span><span class="o">.</span><span class="na">currentThread</span><span class="o">().</span><span class="na">getName</span><span class="o">()</span> <span class="o">+</span> <span class="s">" got resource1 lock."</span><span class="o">);</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">},</span> <span class="s">"Thread 2"</span><span class="o">);</span>

        <span class="n">thread1</span><span class="o">.</span><span class="na">start</span><span class="o">();</span>
        <span class="n">thread2</span><span class="o">.</span><span class="na">start</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h2 id="二使用arthas排查问题">二、使用Arthas排查问题</h2>

<ol>
  <li>启动 Arthas：
<code class="language-plaintext highlighter-rouge">java -jar arthas-boot.jar</code></li>
</ol>

<p><img src="/assets/articleSource/2024-08-18-Arthas/img.png" alt="image.png" />
<img src="/assets/articleSource/2024-08-18-Arthas/img_1.png" alt="image.png" /></p>

<ol>
  <li>查看总体使用情况：
<code class="language-plaintext highlighter-rouge">dashboard</code><img src="/assets/articleSource/2024-08-18-Arthas/img_2.png" alt="image.png" />
可以看到已经有死锁线程了</li>
  <li>查看总体线程使用情况：
<code class="language-plaintext highlighter-rouge">thread</code><img src="/assets/articleSource/2024-08-18-Arthas/img_3.png" alt="image.png" /><strong>BLOCKED</strong>线程数量：2，并且显示了具体的<strong>BLOCKED</strong>线程</li>
  <li>定位死锁的位置：
<code class="language-plaintext highlighter-rouge">thread -b</code><img src="/assets/articleSource/2024-08-18-Arthas/img_4.png" alt="image.png" /></li>
</ol>

<p>输出解释：</p>

<ul>
  <li><strong>线程信息</strong>：<code class="language-plaintext highlighter-rouge">Thread 2</code> 的ID为22，当前状态为<code class="language-plaintext highlighter-rouge">BLOCKED</code>。这意味着它正在等待获取某个对象锁。</li>
  <li><strong>阻塞原因</strong>：<code class="language-plaintext highlighter-rouge">Thread 2</code>被阻塞是因为需要获取的对象<code class="language-plaintext highlighter-rouge">java.util.ArrayLIst@1d0be4d0</code>正被另一个线程所持有。</li>
  <li><strong>锁持有者</strong>：该对象锁<code class="language-plaintext highlighter-rouge">java.util.ArrayLIst@1d0be4d0</code>目前被<code class="language-plaintext highlighter-rouge">Thread 1</code>（ID为21）持有。</li>
  <li><code class="language-plaintext highlighter-rouge">Thread 1</code><strong>的状态</strong>：虽然输出信息没有直接展示<code class="language-plaintext highlighter-rouge">Thread 1</code>的状态，但从上下文可以推断，<code class="language-plaintext highlighter-rouge">Thread 1</code> locak了<code class="language-plaintext highlighter-rouge">java.util.ArrayLIst@1d0be4d0</code>。<code class="language-plaintext highlighter-rouge">Thread 2</code> lock了<code class="language-plaintext highlighter-rouge">java.util.ArrayLIst@1f2efe1c</code></li>
  <li><strong>相互阻塞</strong>：最关键的信息是<code class="language-plaintext highlighter-rouge">ownd by'thread 1</code> ，指出<code class="language-plaintext highlighter-rouge">Thread 2</code>尝试获取的锁被<code class="language-plaintext highlighter-rouge">Thread 1</code>持有，同时说明<code class="language-plaintext highlighter-rouge">Thread 1</code>至少在某一点上也尝试获取<code class="language-plaintext highlighter-rouge">Thread 2</code>持有的锁（或者导致了其他形式的循环等待），从而形成了死锁。这里的<code class="language-plaintext highlighter-rouge">but blocks 1 other threads!</code>暗示了这种相互阻塞的关系。</li>
</ul>

<p>到目前为止。基本已经定位到死锁的具体原因和位置了。</p>

<ol>
  <li>查看死锁线程详细信息：
<code class="language-plaintext highlighter-rouge">thread 21</code>
<code class="language-plaintext highlighter-rouge">thread 22</code><img src="/assets/articleSource/2024-08-18-Arthas/img_5.png" alt="image.png" /></li>
</ol>

<p>根据上面的信息，我们可以清晰地看到两个线程<code class="language-plaintext highlighter-rouge">Thread 1</code>和<code class="language-plaintext highlighter-rouge">Thread 2</code>都处于阻塞状态，形成了死锁：</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">Thread 1 (Id=21)</code> 的状态也是<code class="language-plaintext highlighter-rouge">BLOCKED</code>，它在Test.java:38行尝试获取对象<code class="language-plaintext highlighter-rouge">java.util.ArrayLIst@1f2efe1c</code>的锁，而这个锁正被<code class="language-plaintext highlighter-rouge">Thread 2</code>持有。</li>
  <li><code class="language-plaintext highlighter-rouge">Thread 2 (Id=22)</code> 的状态是<code class="language-plaintext highlighter-rouge">BLOCKED</code>，它在Test.java:23行尝试获取对象<code class="language-plaintext highlighter-rouge">java.util.ArrayLIst@1d0be4d0</code>的锁，但这个锁正被<code class="language-plaintext highlighter-rouge">Thread 1</code>持有。</li>
</ul>

<p>这种相互等待对方释放锁的情形正是死锁的经典表现。每个线程都持有一个锁，并尝试获取对方的锁，导致双方都无法继续执行下去。</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">Thread 1</code>在执行到Test.java的第38行时阻塞，等待<code class="language-plaintext highlighter-rouge">java.util.ArrayLIst@1f2efe1c</code>的锁。</li>
  <li><code class="language-plaintext highlighter-rouge">Thread 2</code>在执行到同文件的第23行时阻塞，等待<code class="language-plaintext highlighter-rouge">java.util.ArrayLIst@1d0be4d0</code>的锁。</li>
  <li>两个线程互相等待对方释放锁，形成了死锁。</li>
</ul>

<p>解决这个问题的关键在于打破死锁的四大条件之一。即：</p>

<ol>
  <li>资源不可重用</li>
  <li>循环等待</li>
  <li>资源不可被抢夺</li>
  <li>吃着碗里的看着锅里的</li>
</ol>]]></content><author><name>xyhao</name></author><category term="Java" /><category term="Java" /><category term="Arthas" /><summary type="html"><![CDATA[更多博客请见 我的语雀知识库]]></summary></entry><entry><title type="html">Vector API介绍&amp;amp;简单使用</title><link href="/jekyll-theme-yat/java/2024/08/02/Vector-API%E4%BB%8B%E7%BB%8D&%E7%AE%80%E5%8D%95%E4%BD%BF%E7%94%A8.html" rel="alternate" type="text/html" title="Vector API介绍&amp;amp;简单使用" /><published>2024-08-02T00:00:00+00:00</published><updated>2024-08-02T00:00:00+00:00</updated><id>/jekyll-theme-yat/java/2024/08/02/Vector%20API%E4%BB%8B%E7%BB%8D&amp;%E7%AE%80%E5%8D%95%E4%BD%BF%E7%94%A8</id><content type="html" xml:base="/jekyll-theme-yat/java/2024/08/02/Vector-API%E4%BB%8B%E7%BB%8D&amp;%E7%AE%80%E5%8D%95%E4%BD%BF%E7%94%A8.html"><![CDATA[<blockquote>
  <p>更多博客请见 <a href="https://www.yuque.com/u41117719/xd1qgc">我的语雀知识库</a></p>
  <blockquote>
    <p>我在参与腾讯犀牛鸟开源时，认领了有关Vector API的issue。但找遍全网也找不到关于Vector API具体使用的介绍。于是我简单学习Vector API之后写下这篇简单的介绍供大家参考。个人水平有限。不足之处请大家多多指正。</p>
  </blockquote>

  <p>【JEP咖啡屋18[熟肉]-如何通过Vector API加速并行计算】<a href="https://www.bilibili.com/video/BV1Nh4y1s7dJ?vd_source=6df57b2b8470f0f2c9187d097a219270">https://www.bilibili.com/video/BV1Nh4y1s7dJ?vd_source=6df57b2b8470f0f2c9187d097a219270</a></p>
</blockquote>

<p><a name="x9C3R"></a></p>
<h2 id="介绍">介绍</h2>
<p>Vector API是自JDK16开始孵化的预览功能，至今仍在孵化中（2024.08.02）。<br />它适用于多个数组的混合运算。将数组转化为多组<strong>定长</strong>的向量进行计算以加快计算速度。而不适用于单数组的内部运算。如：w[i] = w[i - 16] ^ w[i - 9] 
<a name="QW5vV"></a></p>
<h2 id="使用">使用</h2>
<p>在使用Vector API中有一个非常重要的概念需要知晓：
<a name="Y0XS5"></a></p>
<h3 id="species">Species</h3>
<p>Species是用于确定你的机器能够将几个数组元素转化为向量的关键变量。一般它的长度在64位到512位之间。即可以将2到16个int数组元素转化为一组向量。或者可以将4到32个shortInt数组元素转化为一组向量。<br />Species.length()与机器有关。但我们也可以手动指定。<br />定义一个64位的VectorSpecies物种如下：<br /><code class="language-plaintext highlighter-rouge">private static final VectorSpecies&lt;Integer&gt; SPECIES = IntVector.SPECIES_64;</code></p>

<p><a name="IJQSt"></a></p>
<h3 id="从数组中创建向量">从数组中创建向量</h3>
<p>IntVector中提供了一个创建向量的方法fromArray<br />方法签名如下：<br /><code class="language-plaintext highlighter-rouge">IntVector fromArray(VectorSpecies&lt;Integer&gt; species,int[] a, int offset)</code></p>

<ul>
  <li>创建向量必须根据Species创建</li>
  <li>从数组a中创建向量</li>
  <li>offset表示从数组第几位开始创建向量
<a name="il9lc"></a>
    <h3 id="向量写回数组">向量写回数组</h3>
    <p>IntVector中提供了一个写回数组的方法intoArray<br />方法签名：<code class="language-plaintext highlighter-rouge">void intoArray(int[] a, int offset)</code><br />
offset表示从数组第几位开始写入。</p>
  </li>
</ul>

<p><a name="sKbf1"></a></p>
<h3 id="mask">mask</h3>
<p>在标准情况下，并不是每个数组的所有元素都能转化为向量的<br />
前文提到了：Vector API将数组转化为多组<strong>定长</strong>的向量。完全转化需要数组长度能被Species.length整除<br />
但不是每个数组长度都能被Species.length整除<br /></p>

<p>比如数组array = [0, 1, 2, 3, 4………35]，Species.length = 16。只有前32个元素能够被转化为向量，
剩下的子数组[32, 33, 34, 35]则不能够被转化<br />
在<code class="language-plaintext highlighter-rouge">fromArray</code>方法内部对offset有检查。如果<code class="language-plaintext highlighter-rouge">offset &gt;= array.length - Species.length</code>,那么会抛出indexOutOfLength异常。<br /></p>

<p>因此，对于剩下的元素，一般推荐使用原始方式计算。<br />
但<code class="language-plaintext highlighter-rouge">formArray</code>方法也有一个重载版本可以将剩余元素也转化为向量。<br />重载版本的签名如下<br />
<code class="language-plaintext highlighter-rouge">IntVector fromArray(VectorSpecies&lt;Integer&gt; species,int[] a, int offset,VectorMask&lt;Integer&gt; m)</code><br />
多传入一个mask变量，即可做到即使<code class="language-plaintext highlighter-rouge">offset &gt;= array.length - Species.length</code>，也能转化为向量。但需要<code class="language-plaintext highlighter-rouge">offset &lt; array.length</code><br />
mask变量使用示例如下：</p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/*
    SPECIES.length() = 16
    Array.length = 35
*/</span>
<span class="k">for</span> <span class="o">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="o">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="nc">Array</span><span class="o">.</span><span class="na">length</span><span class="o">;</span> <span class="n">i</span> <span class="o">+=</span> <span class="no">SPECIES</span><span class="o">.</span><span class="na">length</span><span class="o">())</span> <span class="o">{</span>
  <span class="kt">var</span> <span class="n">mask</span> <span class="o">=</span> <span class="no">SPECIES</span><span class="o">.</span><span class="na">indexInRange</span><span class="o">(</span><span class="n">i</span><span class="o">,</span> <span class="n">w</span><span class="o">.</span><span class="na">length</span><span class="o">);</span>
  <span class="nc">IntVector</span> <span class="n">v</span> <span class="o">=</span> <span class="nc">IntVector</span><span class="o">.</span><span class="na">fromArray</span><span class="o">(</span><span class="no">SPECIES</span><span class="o">,</span> <span class="nc">Array</span><span class="o">,</span> <span class="n">i</span> <span class="o">-</span> <span class="mi">16</span><span class="o">,</span> <span class="n">mask</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>
<p>在代码<code class="language-plaintext highlighter-rouge">var mask = SPECIES.indexInRange(i, array.length);</code>中，定义变量的var关键字自JDK10开始引入。它允许你在声明变量时省略其类型，编译器会自动推断出其类型。类似于lambda表达式的无类型参数。</p>

<p><a name="xV4bn"></a></p>
<h3 id="lanwise方法">lanwise方法</h3>
<p>lanewise直接翻译就是车道交叉。在Vector API中，向量被视为车道，不同向量之间的运算被视为车道交叉。<br />简单的加减乘除，在<code class="language-plaintext highlighter-rouge">jdk.incubator.vector.Vector</code>类中已经封装了。<br />例如：</p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">var</span> <span class="n">va</span> <span class="o">=</span> <span class="nc">IntVector</span><span class="o">.</span><span class="na">fromArray</span><span class="o">(</span><span class="no">SPECIES</span><span class="o">,</span> <span class="n">a</span><span class="o">,</span> <span class="n">i</span><span class="o">);</span>
<span class="kt">var</span> <span class="n">vb</span> <span class="o">=</span> <span class="nc">IntVector</span><span class="o">.</span><span class="na">fromArray</span><span class="o">(</span><span class="no">SPECIES</span><span class="o">,</span> <span class="n">b</span><span class="o">,</span> <span class="n">i</span><span class="o">);</span>
<span class="kt">var</span> <span class="n">vc</span> <span class="o">=</span> <span class="n">va</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="n">vb</span><span class="o">);</span>
<span class="n">vc</span><span class="o">.</span><span class="na">intoArray</span><span class="o">(</span><span class="n">c</span><span class="o">,</span> <span class="n">i</span><span class="o">);</span>
</code></pre></div></div>

<h3 id="reduceslanes方法">reducesLanes方法</h3>
<p>这是一种归约操作，将向量中各个元素通过某种操作组合起来。如计算一组元素的乘积，总和，最大值，最小值等等</p>

<p>以下为计算一组元素的平方和的代码</p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">IntVector</span> <span class="n">v</span> <span class="o">=</span> <span class="nc">IntVector</span><span class="o">.</span><span class="na">fromArray</span><span class="o">(</span><span class="nc">IVectorSpecies</span><span class="o">.</span><span class="na">SPECIES_128</span><span class="o">,</span> <span class="k">new</span> <span class="kt">int</span><span class="o">[]{</span><span class="mi">1</span><span class="o">,</span> <span class="mi">2</span><span class="o">,</span> <span class="mi">3</span><span class="o">,</span> <span class="mi">4</span><span class="o">});</span>
<span class="kt">var</span> <span class="n">v2</span> <span class="o">=</span> <span class="n">v</span><span class="o">.</span><span class="na">mult</span><span class="o">(</span><span class="n">v</span><span class="o">);</span>
<span class="kt">int</span> <span class="n">sum</span> <span class="o">=</span> <span class="n">v2</span><span class="o">.</span><span class="na">reduceLanes</span><span class="o">(</span><span class="nc">VectorOperators</span><span class="o">.</span><span class="na">ADD</span><span class="o">);</span>
</code></pre></div></div>

<h3 id="更加复杂的操作">更加复杂的操作</h3>
<p>我们应该如何对向量进行复杂的操作呢？<br />在<code class="language-plaintext highlighter-rouge">jdk.incubator.vector.VectorOperators</code>中已经定义了很多常用的操作，与或非、异或、同或等等<br />在向量A的lanewise方法中，传入运算符的定义与另一个向量B。即可让A与B执行特定的运算。如<br /><code class="language-plaintext highlighter-rouge">IntVector temp = wPrev16.lanewise(VectorOperators.XOR, wPrev9)</code><br />向量temp是 wPrev16和wPrev9的异或运算结果。</p>

<p><a name="ZdbWo"></a></p>
<h2 id="如何编译运行使用了vector-api的代码">如何编译运行使用了Vector API的代码？</h2>
<p>由于Vector API至今仍处于孵化期，直接使用必然报错。需要在编译时指定两个参数<code class="language-plaintext highlighter-rouge">--enable-preview</code>和<code class="language-plaintext highlighter-rouge">--add-modules=jdk.incubator.vector</code>。<br />在哪里指定？<br />网络上一些博客说的在pom文件中添加编译参数：</p>
<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;build&gt;</span>
        <span class="nt">&lt;plugins&gt;</span>
            <span class="nt">&lt;plugin&gt;</span>
                <span class="nt">&lt;groupId&gt;</span>org.apache.maven.plugins<span class="nt">&lt;/groupId&gt;</span>
                <span class="nt">&lt;artifactId&gt;</span>maven-compiler-plugin<span class="nt">&lt;/artifactId&gt;</span>
                <span class="nt">&lt;configuration&gt;</span>
                    <span class="nt">&lt;compilerArgs&gt;</span>
                        <span class="nt">&lt;arg&gt;</span>--enable-preview<span class="nt">&lt;/arg&gt;</span>
                        <span class="nt">&lt;arg&gt;</span>--add-modules=jdk.incubator.vector<span class="nt">&lt;/arg&gt;</span>
                    <span class="nt">&lt;/compilerArgs&gt;</span>
                    <span class="nt">&lt;compilerVersion&gt;</span>21<span class="nt">&lt;/compilerVersion&gt;</span>
                    <span class="nt">&lt;source&gt;</span>21<span class="nt">&lt;/source&gt;</span>
                    <span class="nt">&lt;target&gt;</span>21<span class="nt">&lt;/target&gt;</span>
                <span class="nt">&lt;/configuration&gt;</span>
            <span class="nt">&lt;/plugin&gt;</span>
        <span class="nt">&lt;/plugins&gt;</span>

    <span class="nt">&lt;/build&gt;</span>
</code></pre></div></div>
<p>对我而言并没有用。<br />在IDEA的运行配置中添加也没有用：<br /><img src="/assets/articleSource/2024-08-02-VectorAPI/img_1.png" alt="image.png" /><br /><br />
于是我只能使用最原始的办法。在命令行中运行<br /><br /><img src="/assets/articleSource/2024-08-02-VectorAPI/img_2.png" alt="image.png" /><img src="/assets/articleSource/2024-08-02-VectorAPI/img_3.png" alt="img.png" /><br /><br />
我的机器上的JDK版本是21。想使用Vector API最低也要JDK16。<br /><br /></p>]]></content><author><name>xyhao</name></author><category term="Java" /><category term="Java" /><category term="Vector" /><summary type="html"><![CDATA[更多博客请见 我的语雀知识库 我在参与腾讯犀牛鸟开源时，认领了有关Vector API的issue。但找遍全网也找不到关于Vector API具体使用的介绍。于是我简单学习Vector API之后写下这篇简单的介绍供大家参考。个人水平有限。不足之处请大家多多指正。 【JEP咖啡屋18[熟肉]-如何通过Vector API加速并行计算】https://www.bilibili.com/video/BV1Nh4y1s7dJ?vd_source=6df57b2b8470f0f2c9187d097a219270]]></summary></entry></feed>